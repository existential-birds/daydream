"""Shared backend-driver test harness.

Test-support package exposing reusable replay primitives for driving real
``Backend`` drivers through their external boundaries (Codex subprocess /
Claude SDK stream) without duplicating mock-process scaffolding.
"""
