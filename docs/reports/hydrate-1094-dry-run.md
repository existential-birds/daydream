# hydrate-hub dry-run report — issue #1094 pinned archive fixture

Generated by executing the production dry-run stage sequence
(`download_snapshot` → `ingest_bundles` → `dedupe_admitted` →
`enrich_license_evidence` → `restamp_admitted_digests` →
`apply_license_gate` → `resolve_curation_identity` →
`build_import_ledger`) over the pinned archive fixture
(`tests/fixtures/training/build_hub_snapshot.py::build_pinned_snapshot`) with the
pinned policy fixture `tests/fixtures/training/license-policy-pinned-fixture.json`.
Numbers below are the actual measured outputs of that run.

## Inputs

- Source repo: `org/private-ds`
- Pinned snapshot revision: `7e11a1d36bca4cb09b0b0ccad5382c84ff5e5975`
- Policy digest (sha256 of the pinned policy file bytes): `ee9c9029ee19412218c1de9feb5b86bbd15173f336070aefb31a6cb2a76c6393`
- Policy version: `production-1`
- Copyleft opt-ins: none
- Derived v2 curation id: `cur-f03b7a677fa234db`

## Record accounting

- Discovered candidates: **5**
- Admitted batches: **2**
- Rejected batches: **3**
- Accounted (admitted + rejected): **5**
- Incomplete manifests: **0**

Invariant: admitted + rejected == discovered (2 + 3 == 5) — holds.

## License admission summary (per code)

| Bucket | Count |
|--------|-------|
| admitted | 2 |
| c5_excluded | 1 |
| c8_copyleft_unopted | 1 |
| license_evidence_missing | 1 |
| **Total** | **5** |

Stable rejection reason codes observed:

- `c5_excluded_repo`: 1
- `c8_copyleft_unopted`: 1
- `repo_commit_unresolved`: 1

## Per-repository decision counts

| Repo slug | admitted | c5_excluded | c8_copyleft_unopted | license_evidence_missing |
|-----------|----------|-------------|---------------------|--------------------------|
| `acme/copyleft` | 0 | 0 | 1 | 0 |
| `acme/widget` | 2 | 0 | 0 | 0 |
| `getsentry/sentry` | 0 | 1 | 0 | 0 |
| `ghost/nope` | 0 | 0 | 0 | 1 |
| **Total** | **5** | | | |

Invariant: per-repository buckets total == license-adjudicated population
(2 admitted + 3 license-gate rejections == 5) — holds.

## License distribution (adjudicated population)

| SPDX id | Records |
|---------|---------|
| GPL-3.0-only | 1 |
| MIT | 2 |
| none | 2 |
| **Total** | **5** |

Decision identity: `acme/widget` resolves MIT (2 admitted sessions),
`acme/copyleft` resolves GPL-3.0-only (1 c8 rejection). The C5-excluded
(`getsentry/sentry`) and commit-unresolved (`ghost/nope`) decisions carry
`spdx_id=None` — their evidence-level values are deliberately not counted
(the distribution follows decision identity, not evidence values), so they
show as no-spdx rows.

Invariant: distribution total == adjudicated population (2 admitted + 3
license-gate rejections == 5) — holds.

## Enrichment resolver traffic

Distinct repos queried at the authorized source: 4 (`acme/copyleft`, `acme/widget`, `getsentry/sentry`, `ghost/nope`).
Repository commits in the resolution map come from the resolver; the Hub
dataset revision is never recorded as a repository commit.

This dry-run performed no publication: no curated bytes, no `_SUCCESS`
marker. Per the runbook, the non-dry publication is gated on this
completed dry-run and its full record accounting.
