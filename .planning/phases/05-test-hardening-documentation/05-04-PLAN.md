---
phase: 05-test-hardening-documentation
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified: []
autonomous: false
requirements:
  - TEST-01
  - TEST-05

must_haves:
  truths:
    - "make test passes with 0 failures and >= 570 tests collected"
    - "make lint passes cleanly"
    - "make typecheck passes cleanly"
    - "No full-tree snapshot equality in any trajectory/redaction test file"
    - "All 13 Phase 5 requirements (TEST-01..07, DOCS-01..06) are verifiably met"
  artifacts: []
  key_links: []
---

<objective>
Run the full CI check suite (lint + typecheck + test) and verify all 13 Phase 5 requirements are met. This is the migration-complete gate.

Purpose: Phase 5 success criteria require "all 343 pre-existing tests passing plus new tests" and "schema-validity + behavior-predicate patterns throughout." This plan runs the full verification and presents results for human sign-off.

Output: Verified migration-complete signal.
</objective>

<execution_context>
@.planning/phases/05-test-hardening-documentation/05-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full CI check suite and verify requirement coverage</name>
  <files></files>
  <read_first>
    - tests/test_multi_turn_tokens.py (verify it exists and has TEST-06 gate tests)
    - tests/test_subagent_shapes.py (verify it exists and has TEST-07 shape tests)
    - README.md (lines 187-210 only -- verify Trajectory Output section exists)
    - CHANGELOG.md (lines 8-40 only -- verify [0.14.0] entry exists)
    - CLAUDE.md (lines 28-70 only -- verify trajectory.py referenced, --debug removed)
    - daydream/atif/NOTICE (verify Apache-2.0 attribution)
  </read_first>
  <action>
Run the complete verification sequence:

1. `uv run ruff check daydream` -- lint must pass
2. `uv run mypy daydream` -- typecheck must pass
3. `uv run pytest -v --tb=short` -- all tests must pass, count >= 570

Then verify each requirement:

**TEST-01:** Test count >= 564 (343 baseline + 221 from phases 1-4), 0 failures
**TEST-02:** `grep -c 'def test_' tests/test_trajectory.py` >= 30 (recorder lifecycle + step coalescing + tool-call correlation + validator round-trip)
**TEST-03:** `grep -c 'def test_' tests/test_redaction.py` >= 20 (all 4 pattern categories + cross-surface + failure mode)
**TEST-04:** `grep -c 'golden_path' tests/test_atif_vendor_smoke.py` >= 1 (parametrized over golden fixtures)
**TEST-05:** `grep -rn 'assert.*traj.*== {' tests/test_trajectory.py tests/test_redaction.py tests/test_atif_vendor_smoke.py tests/test_trajectory_fixture.py tests/test_multi_turn_tokens.py tests/test_subagent_shapes.py` returns empty (no full-tree snapshot equality)
**TEST-06:** `uv run pytest tests/test_multi_turn_tokens.py -v` passes with >= 3 tests
**TEST-07:** `uv run pytest tests/test_subagent_shapes.py -v` passes with >= 6 tests
**DOCS-01:** `grep '## Trajectory Output' README.md` matches
**DOCS-02:** `grep 'REDACTED' README.md` matches
**DOCS-03:** `grep 'Harbor' README.md` matches
**DOCS-04:** `grep '## \[0\.14\.0\]' CHANGELOG.md` matches
**DOCS-05:** `grep 'Apache' daydream/atif/NOTICE` matches
**DOCS-06:** `grep 'trajectory.py' CLAUDE.md` matches

If any check fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/ka/github/existential-birds/daydream && uv run ruff check daydream && uv run mypy daydream && uv run pytest -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check daydream` exits 0 (no lint errors)
    - `uv run mypy daydream` exits 0 (no type errors)
    - `uv run pytest -v` exits 0 with >= 570 tests collected, 0 failures
    - All 13 requirement grep checks above return expected results
  </acceptance_criteria>
  <done>Full CI suite passes; all 13 Phase 5 requirements verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 5 (Test Hardening + Documentation) is complete. The ATIF v1.6 migration is finished across all 5 phases:
    - Phase 1: Vendored ATIF models + validator + golden fixtures
    - Phase 2: TrajectoryRecorder + event enrichment + event-to-ATIF mapping
    - Phase 3: Subagent wiring for parallel fork/sibling trajectories
    - Phase 4: Legacy debug logging removal + redaction + CLI surface (--trajectory)
    - Phase 5: Test audit/gap-fill + new TEST-06/TEST-07 tests + README/CHANGELOG/CLAUDE.md docs
  </what-built>
  <how-to-verify>
    1. Run `make check` (or `make lint && make typecheck && make test`) and confirm all checks pass
    2. Run `daydream --help` and confirm `--trajectory` flag is documented and `--debug` is absent
    3. Open README.md and verify the "Trajectory Output" section reads clearly
    4. Open CHANGELOG.md and verify the [0.14.0] entry accurately describes the migration
    5. Optionally: run `daydream /path/to/small/project --python` and inspect the resulting `.daydream/trajectory.json` to confirm it's a valid ATIF trajectory
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 5 complete, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries. Verification-only plan -- reads existing files and runs existing test/lint commands.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | N/A | N/A | N/A | No new threats -- verification only |
</threat_model>

<verification>
1. `make check` exits 0 (lint + typecheck + test)
2. All 13 requirement IDs have verifiable evidence
3. Human sign-off on migration completeness
</verification>

<success_criteria>
- Full CI suite (lint + typecheck + test) passes
- All 13 Phase 5 requirements confirmed met
- Human approves migration completeness
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-hardening-documentation/05-04-SUMMARY.md`
</output>
