---
phase: 05-test-hardening-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_trajectory.py
  - tests/test_redaction.py
  - tests/test_atif_vendor_smoke.py
  - tests/test_trajectory_fixture.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05

must_haves:
  truths:
    - "Every existing trajectory test uses schema-validity + behavior-predicate pattern (no full-tree snapshot equality)"
    - "test_trajectory.py covers recorder lifecycle, step coalescing, tool-call correlation, and validator round-trip with no gaps against TEST-02"
    - "test_redaction.py covers each Redactor pattern category (API keys, JWTs, user paths, env vars) with positive and negative cases"
    - "test_atif_vendor_smoke.py parametrizes over all golden fixtures in tests/fixtures/atif_golden/"
    - "All 564+ existing tests still pass after any modifications"
  artifacts:
    - path: "tests/test_trajectory.py"
      provides: "Recorder lifecycle, step coalescing, tool-call correlation, validator round-trip tests"
      contains: "atif_validate"
    - path: "tests/test_redaction.py"
      provides: "Per-pattern redaction positive and negative cases"
      contains: "REDACTED_API_KEY"
    - path: "tests/test_atif_vendor_smoke.py"
      provides: "Parametrized golden fixture validation"
      contains: "golden_path"
    - path: "tests/test_trajectory_fixture.py"
      provides: "ContextVar leak detection and autouse fixture isolation"
      contains: "get_current_recorder"
  key_links:
    - from: "tests/test_trajectory.py"
      to: "daydream/atif/__init__.py"
      via: "from daydream.atif import validate as atif_validate"
      pattern: "atif_validate"
    - from: "tests/test_redaction.py"
      to: "daydream/trajectory.py"
      via: "from daydream.trajectory import Redactor"
      pattern: "Redactor"
---

<objective>
Audit existing trajectory, redaction, golden-fixture, and fixture-isolation tests against TEST-01 through TEST-05 requirements. Fix any TEST-05 violations (full-tree snapshot equality patterns). Fill coverage gaps for TEST-02 (recorder lifecycle), TEST-03 (redaction patterns), and TEST-04 (golden fixtures). Confirm TEST-01 (all pre-existing tests pass).

Purpose: Phase 5 is audit-first per D-01. Phases 1-4 already landed 221 new tests (564 total). This plan reads the 4 existing test files, maps each test function to its requirement, identifies uncovered behaviors, and fills gaps. It also enforces TEST-05 (the D-18 schema-validity + behavior-predicate pattern ban on full-tree snapshot equality).

Output: Audited and gap-filled test files with full TEST-01 through TEST-05 coverage.
</objective>

<execution_context>
@.planning/phases/05-test-hardening-documentation/05-CONTEXT.md
@.planning/phases/05-test-hardening-documentation/05-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
From tests/test_trajectory.py (existing helpers):
```python
def _make_recorder(tmp_path: Path, *, agent_model_name: str = "opus") -> TrajectoryRecorder:
    return TrajectoryRecorder(
        path=tmp_path / ".daydream" / "trajectory.json",
        run_flow=DaydreamRunFlow.NORMAL,
        target_dir=tmp_path,
        agent_model_name=agent_model_name,
    )

def _read_trajectory(path: Path) -> dict[str, Any]:
    return json.loads(path.read_text(encoding="utf-8"))
```

From daydream/atif/__init__.py:
```python
from daydream.atif.validator import TrajectoryValidator
def validate(source, *, validate_images: bool = False) -> bool: ...
```

From daydream/trajectory.py:
```python
class TrajectoryRecorder: ...
class Invocation: ...
class Redactor: ...
class DaydreamPhase(Enum): ...
class DaydreamRunFlow(Enum): ...
def get_current_recorder() -> TrajectoryRecorder | None: ...
def now_iso() -> str: ...
def _safe_descriptor(raw: str) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing test files and map coverage to TEST-01..05</name>
  <files>tests/test_trajectory.py, tests/test_redaction.py, tests/test_atif_vendor_smoke.py, tests/test_trajectory_fixture.py</files>
  <read_first>
    - tests/test_trajectory.py (1019 lines -- full read, map each test function to TEST-02 sub-requirements: recorder lifecycle, step coalescing, tool-call correlation, validator round-trip)
    - tests/test_redaction.py (418 lines -- full read, map each test to TEST-03 pattern categories: API keys sk-*/ghp_*/xoxb-*/AKIA*, JWTs eyJ*, user paths, env vars)
    - tests/test_atif_vendor_smoke.py (67 lines -- full read, verify parametrization over all golden fixtures for TEST-04)
    - tests/test_trajectory_fixture.py (52 lines -- full read, verify ContextVar leak detection for TEST-05)
    - tests/test_agent_recorder_integration.py (378 lines -- full read, identify any TEST-02 coverage that lives here instead of test_trajectory.py)
  </read_first>
  <action>
Read all 5 test files. For each test function, record which TEST requirement it covers:

TEST-02 sub-requirements checklist:
- Recorder lifecycle (__aenter__/__aexit__, file write on clean exit, ContextVar set/clear)
- Step coalescing (TextEvent chunks concatenate, flush on result boundary)
- Tool-call correlation (ToolCall.tool_call_id matches ObservationResult.source_call_id in same step)
- Validator round-trip (produced trajectory passes atif_validate)

TEST-03 sub-requirements checklist:
- API key patterns: sk-*, ghp_*, xoxb-*, AKIA* (positive + negative)
- JWT tokens: eyJ* (positive + negative)
- User paths: /Users/<name>/, /home/<name>/, C:\Users\<name>\ (positive + negative for relative paths)
- Env vars: KEY=value where key contains SECRET/TOKEN/PASSWORD/etc (positive + negative for non-secret env vars)
- Cross-surface coverage: ToolCall.arguments, ObservationResult.content, Step.message, Step.reasoning_content
- Failure mode: redact-or-omit, never raw-pass-through (REDA-05)

TEST-04 checklist:
- Parametrized over ALL .json files in tests/fixtures/atif_golden/ (excluding _invalid/)
- Negative fixture (deliberately broken) is caught by validator

TEST-05 checklist:
- Scan ALL test files for `assert.*==.*{` or `assert.*==.*\[` patterns that compare full trajectory dicts/lists
- Flag any `assert trajectory == expected_dict` or equivalent full-tree snapshot comparisons
- Confirm tests follow schema-validity (atif_validate) + behavior-predicate (1-2 specific assertions) pattern

Produce a coverage map as inline comments at the top of each file documenting which TEST-XX is covered by which test function. Do NOT write this to a file -- keep it as working notes for the next task.

Identify specific gaps (test functions that need to be added or modified).
  </action>
  <verify>
    <automated>cd /Users/ka/github/existential-birds/daydream && uv run pytest tests/test_trajectory.py tests/test_redaction.py tests/test_atif_vendor_smoke.py tests/test_trajectory_fixture.py tests/test_agent_recorder_integration.py -v --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 5 test files read completely
    - Coverage gaps identified against TEST-02, TEST-03, TEST-04, TEST-05 checklists
    - Any full-tree snapshot equality violations (TEST-05) identified with line numbers
    - Working notes list specific gap-filling actions for Task 2
  </acceptance_criteria>
  <done>Coverage audit complete with identified gaps ready for Task 2 to fill</done>
</task>

<task type="auto">
  <name>Task 2: Fill coverage gaps and fix TEST-05 violations</name>
  <files>tests/test_trajectory.py, tests/test_redaction.py, tests/test_atif_vendor_smoke.py, tests/test_trajectory_fixture.py</files>
  <read_first>
    - tests/test_trajectory.py (already in context from Task 1)
    - tests/test_redaction.py (already in context from Task 1)
    - tests/test_atif_vendor_smoke.py (already in context from Task 1)
    - tests/test_trajectory_fixture.py (already in context from Task 1)
    - .planning/phases/05-test-hardening-documentation/05-PATTERNS.md (for pattern templates)
  </read_first>
  <action>
Based on the audit from Task 1, fill gaps and fix violations:

**TEST-05 fixes (if any violations found):**
Replace any `assert trajectory == expected_dict` or full-tree snapshot comparisons with the schema-validity + behavior-predicate pattern:
```python
# Step 1: validate schema
assert atif_validate(traj, validate_images=False) is True
# Step 2: assert specific behavioral predicate
agent_steps = [s for s in traj["steps"] if s["source"] == "agent"]
assert len(agent_steps) >= 1
assert "expected_text" in agent_steps[0]["message"]
```

**TEST-02 gap fills (if any found):**
Add tests to test_trajectory.py following the existing `_make_recorder` + `_read_trajectory` helper pattern. Each new test must:
1. Create a recorder via `_make_recorder(tmp_path)`
2. Drive it with events via `async with recorder:` + `async with recorder.invocation(phase=DaydreamPhase.REVIEW) as inv:`
3. Assert `atif_validate(traj, validate_images=False) is True`
4. Assert 1-2 behavioral predicates

**TEST-03 gap fills (if any found):**
Add tests to test_redaction.py following the existing `_user_step` / `_agent_step` helper pattern. Use `Redactor().redact_step(step)` and assert the secret is replaced with the type-specific token and clean input passes through.

**TEST-04 gap fills (if any found):**
Extend test_atif_vendor_smoke.py to ensure every .json file under `tests/fixtures/atif_golden/` (excluding `_invalid/`) is covered by the parametrized test.

**TEST-01 confirmation:**
After all modifications, run `uv run pytest -v` and confirm all tests pass. The test count should be >= 564 (existing) plus any new gap-fill tests.

All new tests must follow the project conventions:
- `async def test_<behavior>(tmp_path: Path) -> None:` signature
- Google-style single-line docstring referencing the requirement ID
- No `@pytest.mark.asyncio` needed (asyncio_mode = "auto")
- Import from `daydream.atif import validate as atif_validate`
- Import from `daydream.trajectory import TrajectoryRecorder, DaydreamPhase, DaydreamRunFlow, Redactor, now_iso`
  </action>
  <verify>
    <automated>cd /Users/ka/github/existential-birds/daydream && uv run pytest tests/test_trajectory.py tests/test_redaction.py tests/test_atif_vendor_smoke.py tests/test_trajectory_fixture.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - No test function in test_trajectory.py, test_redaction.py, test_atif_vendor_smoke.py, or test_trajectory_fixture.py uses `assert trajectory == ` or `assert traj == ` full-dict comparison (TEST-05)
    - test_trajectory.py contains tests covering: recorder __aenter__/__aexit__ lifecycle, TextEvent chunk coalescing, ToolCall/ObservationResult same-step correlation, atif_validate round-trip (TEST-02)
    - test_redaction.py contains tests covering: sk-*, ghp_*, xoxb-*, AKIA*, eyJ*, /Users/*/home/*/C:\Users\*, env-var KEY=value, ToolCall.arguments surface, ObservationResult.content surface, Step.reasoning_content surface, failure mode (TEST-03)
    - test_atif_vendor_smoke.py parametrizes over all .json files in tests/fixtures/atif_golden/ excluding _invalid/ (TEST-04)
    - `uv run pytest -v` exits 0 with >= 564 tests collected (TEST-01)
    - `grep -rn 'assert.*traj.*==' tests/test_trajectory.py tests/test_redaction.py tests/test_atif_vendor_smoke.py tests/test_trajectory_fixture.py | grep -v 'is True' | grep -v 'is False' | grep -v '==' | wc -l` returns 0 (no full-tree snapshot equality)
  </acceptance_criteria>
  <done>All TEST-01 through TEST-05 requirements covered; zero full-tree snapshot equality violations; all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. Phase 5 is tests + documentation only -- no production code changes, no new attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I (Information Disclosure) | test fixtures | accept | Test files may contain example secret patterns (e.g., `sk-test-12345`) but these are synthetic test data, not real credentials. Acceptable for test corpus. |
</threat_model>

<verification>
1. `uv run pytest tests/test_trajectory.py tests/test_redaction.py tests/test_atif_vendor_smoke.py tests/test_trajectory_fixture.py -v` -- all pass
2. `grep -c 'atif_validate' tests/test_trajectory.py` -- returns > 0 (schema-validity pattern used)
3. No `assert trajectory == {` or `assert traj == {` full-dict comparisons in any modified file
4. `uv run pytest -v --tb=short 2>&1 | tail -3` -- shows >= 564 tests, 0 failures
</verification>

<success_criteria>
- TEST-01: Full test suite passes (>= 564 tests, 0 failures)
- TEST-02: test_trajectory.py covers recorder lifecycle + step coalescing + tool-call correlation + validator round-trip
- TEST-03: test_redaction.py covers all 4 pattern categories with positive + negative cases + all 4 ATIF surfaces
- TEST-04: test_atif_vendor_smoke.py parametrizes over all golden fixtures
- TEST-05: Zero full-tree snapshot equality patterns in any trajectory/redaction test file
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-hardening-documentation/05-01-SUMMARY.md`
</output>
