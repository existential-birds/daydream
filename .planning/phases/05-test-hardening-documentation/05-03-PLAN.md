---
phase: 05-test-hardening-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
  - CLAUDE.md
  - daydream/atif/NOTICE
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06

must_haves:
  truths:
    - "README.md has a Trajectory Output section documenting format, default path, --trajectory flag, and redaction policy"
    - "README.md links to docs/reference/atif_format.md for full format spec"
    - "README.md has consumer integration pointers (Harbor, replay viewers, SFT-RL)"
    - "CHANGELOG.md has a [0.14.0] entry with Breaking, Added, Removed subsections"
    - "CLAUDE.md mentions daydream/trajectory.py and the trajectory format"
    - "daydream/atif/NOTICE is complete with Apache-2.0 attribution"
  artifacts:
    - path: "README.md"
      provides: "Trajectory output documentation, redaction policy, consumer paths"
      contains: "Trajectory Output"
    - path: "CHANGELOG.md"
      provides: "Versioned changelog entry for the ATIF migration"
      contains: "[0.14.0]"
    - path: "CLAUDE.md"
      provides: "Updated project instructions with trajectory module reference"
      contains: "trajectory.py"
    - path: "daydream/atif/NOTICE"
      provides: "Apache-2.0 attribution for vendored Harbor code"
      contains: "Apache"
  key_links:
    - from: "README.md"
      to: "docs/reference/atif_format.md"
      via: "markdown link"
      pattern: "atif_format.md"
---

<objective>
Update all documentation files for the ATIF migration: README.md (trajectory output section with format, flags, redaction, consumer links), CHANGELOG.md (0.14.0 entry with breaking/added/removed), CLAUDE.md (trajectory module reference), and daydream/atif/NOTICE (verify completeness).

Purpose: Migration-complete documentation signal. A reader can run `daydream <target>`, open the resulting trajectory, and follow README links to validate and replay it. CHANGELOG documents the breaking CLI change for existing users.

Output: Four updated documentation files completing DOCS-01 through DOCS-06.
</objective>

<execution_context>
@.planning/phases/05-test-hardening-documentation/05-CONTEXT.md
@.planning/phases/05-test-hardening-documentation/05-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
From README.md current structure (221 lines):
- Lines 1-8: Title + badge + description
- Lines 9-19: Features list
- Lines 21-38: Prerequisites + Beagle install
- Lines 40-94: Installation + Usage + More Examples + CLI Options table
- Lines 124-186: How It Works (Standard Review, TTT, PR Feedback)
- Lines 187-194: Output Files table
- Lines 195-210: Architecture tree
- Lines 212-221: Dependencies + License

From CHANGELOG.md current structure (377 lines):
- Line 1: # Changelog
- Lines 2-7: Preamble
- Line 8: ## [Unreleased]
- Line 10: ## [0.13.1] - 2026-04-26
- Bottom: [unreleased]: https://...compare/v0.13.1...HEAD link references

From CLAUDE.md relevant sections:
- Line 30: daydream --debug /path/to/project  # Enable debug logging
- Lines 44-55: Module Responsibilities (missing trajectory.py)
- Lines 64-67: Key Patterns (references to debug logging)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md (DOCS-01, DOCS-02, DOCS-03) and CHANGELOG.md (DOCS-04)</name>
  <files>README.md, CHANGELOG.md</files>
  <read_first>
    - README.md (221 lines -- full read, identify exact insertion points)
    - CHANGELOG.md (lines 1-70 -- existing entry format and link references at bottom)
    - CHANGELOG.md (lines 355-377 -- bottom link references)
    - docs/reference/atif_format.md (lines 1-30 -- title and intro, to reference correctly in README link)
  </read_first>
  <action>
**README.md changes:**

1. **Update Output Files table** (around line 187-194):
   - Remove the `.review-debug-{timestamp}.log` row (debug system is gone)
   - Add trajectory row:
     ```
     | `.daydream/trajectory.json` | ATIF v1.6 trajectory recording (always written; customize path with `--trajectory`) |
     ```
   - Keep `.review-output.md` and `.daydream/plan-{timestamp}.md` rows unchanged

2. **Insert new "## Trajectory Output" section** after the Output Files table (before Architecture):
   Per D-05, this is 3-4 sentences max with a link. Content:
   ```markdown
   ## Trajectory Output

   Every daydream run produces an [ATIF v1.6](docs/reference/atif_format.md) trajectory file at `<target>/.daydream/trajectory.json`. The trajectory captures the full agent interaction history — prompts, responses, tool calls, observations, and per-step token/cost metrics. Use `--trajectory <path>` to write to a custom location.

   Sensitive content is automatically redacted before writing: API keys (`sk-*`, `ghp_*`, `xoxb-*`, `AKIA*`), JWT tokens, username segments in file paths, and `.env`-style secret values are replaced with type-specific `[REDACTED_*]` tokens.

   **Consumer integration:**
   - Validate trajectories with [Harbor](https://github.com/laude-institute/harbor)'s trajectory validator
   - Replay in any ATIF-compatible viewer
   - Use as training data for SFT/RL pipelines (trajectories are machine-parseable by design)
   ```

3. **Update CLI Options table** (around lines 40-94):
   - Remove `--debug` row if present
   - Add `--trajectory <path>` row with description: "Write trajectory to custom path (default: `<target>/.daydream/trajectory.json`)"

4. **Update Features list** (lines 9-19):
   - Add bullet: "ATIF v1.6 trajectory recording with automatic secret redaction"

**CHANGELOG.md changes:**

1. **Insert `[0.14.0]` entry** between `[Unreleased]` (line 8) and `[0.13.1]` (line 10). Per D-08 (version 0.14.0) and D-09 (single entry with grouped subsections):
   ```markdown
   ## [0.14.0] - 2026-04-28

   ### Breaking

   - **cli:** Remove `--debug` flag; use `--trajectory <path>` to control trajectory output location. Daydream no longer produces `.review-debug-{timestamp}.log` files.

   ### Added

   - **trajectory:** Every run now produces an [ATIF v1.6](docs/reference/atif_format.md) trajectory file at `<target>/.daydream/trajectory.json` capturing the full agent interaction history, tool I/O, and per-step token/cost metrics.
   - **cli:** Add `--trajectory <path>` flag to write trajectories to a custom location. Trajectories are always written; the flag only controls the output path.
   - **redaction:** Automatic secret redaction applied to all trajectory content — API keys, JWT tokens, file paths with usernames, and `.env`-style secret values are replaced with `[REDACTED_*]` tokens before writing.
   - **trajectory:** Parallel fan-out flows (fix-parallel, deep-mode per-stack, exploration specialists) produce sibling trajectory files linked from the root via `subagent_trajectory_ref`.
   - **trajectory:** SIGINT/SIGTERM mid-run flushes a partial trajectory to `<path>.partial`.

   ### Removed

   - **agent:** Remove `_log_debug()` debug logging system and all prefix-tagged log lines (`[TEXT]`, `[TOOL_USE]`, `[COST]`, etc.). Trajectory recording replaces all debug observability.
   - **agent:** Remove `AgentState.debug_log` field and `set_debug_log()`/`get_debug_log()` accessors.
   - **runner:** Remove `.review-debug-{timestamp}.log` file initialization.
   ```

2. **Update bottom link references** -- add `[0.14.0]` compare link:
   ```markdown
   [unreleased]: https://github.com/existential-birds/daydream/compare/v0.14.0...HEAD
   [0.14.0]: https://github.com/existential-birds/daydream/compare/v0.13.1...v0.14.0
   ```
   Update the `[unreleased]` link to compare from v0.14.0 instead of v0.13.1.
  </action>
  <verify>
    <automated>cd /Users/ka/github/existential-birds/daydream && grep -c 'Trajectory Output' README.md && grep -c '\[0\.14\.0\]' CHANGELOG.md && grep -c 'atif_format.md' README.md && grep -c 'REDACTED' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## Trajectory Output' README.md` returns 1
    - `grep -c 'atif_format.md' README.md` returns >= 1 (link to format doc)
    - `grep -c 'REDACTED' README.md` returns >= 1 (redaction policy mentioned)
    - `grep -c 'Harbor' README.md` returns >= 1 (consumer integration pointer)
    - `grep -c 'trajectory.json' README.md` returns >= 1 (default path documented)
    - `grep -c '\-\-trajectory' README.md` returns >= 1 (flag documented)
    - `grep -c '\-\-debug' README.md` returns 0 (old flag removed from docs)
    - `grep -c '## \[0\.14\.0\]' CHANGELOG.md` returns 1
    - `grep -c '### Breaking' CHANGELOG.md` returns >= 1
    - `grep -c '### Added' CHANGELOG.md` returns >= 1 (in 0.14.0 section)
    - `grep -c '### Removed' CHANGELOG.md` returns >= 1 (in 0.14.0 section)
    - `grep -c '_log_debug' CHANGELOG.md` returns >= 1 (removal documented)
    - `grep 'compare/v0.14.0' CHANGELOG.md` returns the link reference
  </acceptance_criteria>
  <done>README has Trajectory Output section with format, flag, redaction, and consumer links. CHANGELOG has [0.14.0] entry with Breaking/Added/Removed subsections.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md (DOCS-06) and verify daydream/atif/NOTICE (DOCS-05)</name>
  <files>CLAUDE.md, daydream/atif/NOTICE</files>
  <read_first>
    - CLAUDE.md (full read -- identify all --debug references and the Module Responsibilities section where trajectory.py should be added)
    - daydream/atif/NOTICE (40 lines -- verify Apache-2.0 attribution completeness for DOCS-05)
  </read_first>
  <action>
**CLAUDE.md changes (DOCS-06):**

1. **Update Commands section** (around line 30):
   - Replace `daydream --debug /path/to/project       # Enable debug logging` with:
     `daydream --trajectory /tmp/out.json /path/to/project  # Custom trajectory path`
   - The existing `daydream /path/to/project --python` and other examples stay unchanged

2. **Update Module Responsibilities section** (around lines 44-55):
   - Add trajectory.py entry between agent.py and ui.py:
     `- **trajectory.py**: ATIF v1.6 trajectory recorder, `TrajectoryRecorder` with ContextVar propagation, `Invocation` per-run_agent scope, `Redactor` for secret scrubbing, `DaydreamPhase`/`DaydreamRunFlow` enums`

3. **Update Key Patterns section** (around lines 64-67):
   - Remove or update any references to `_log_debug` or debug logging
   - Add: `- Trajectory recording via `TrajectoryRecorder` propagated through `ContextVar`; parallel fan-outs use `recorder.fork()` for sibling trajectory files`

4. **Update Global state reference** (around line 71):
   - Remove mention of `debug_log` from `AgentState` description if present
   - Add `_RECORDER_VAR` ContextVar mention alongside `AgentState`: `Trajectory recorder propagated via ContextVar in `daydream/trajectory.py` (separate from AgentState singleton)`

5. **Update Architecture section** if it references debug logging -- replace with trajectory references.

6. **Update the Logging section** (if one exists):
   - Remove references to `[PROMPT]`, `[TEXT]`, `[TOOL_USE]`, `[COST]` prefix-tagged debug lines
   - Note that observability is now via ATIF trajectory files

**daydream/atif/NOTICE verification (DOCS-05):**

Read the existing NOTICE file (40 lines). Verify it contains:
- Apache-2.0 license reference
- Attribution to Harbor / Laude Institute
- Source commit hash or version reference
- List of vendored files

If all present, no changes needed. If any item is missing, add it. The file was created in Phase 1 and should be complete -- this is a verification-only step.
  </action>
  <verify>
    <automated>cd /Users/ka/github/existential-birds/daydream && grep -c 'trajectory.py' CLAUDE.md && grep -c 'debug_log' CLAUDE.md && grep -c 'Apache' daydream/atif/NOTICE && grep -c '_log_debug' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'trajectory.py' CLAUDE.md` returns >= 1 (module mentioned in responsibilities)
    - `grep -c 'TrajectoryRecorder' CLAUDE.md` returns >= 1 (key abstraction documented)
    - `grep -c 'ContextVar' CLAUDE.md` returns >= 1 (propagation mechanism mentioned)
    - `grep -c '\-\-debug' CLAUDE.md` returns 0 (old flag removed from examples)
    - `grep -c '_log_debug' CLAUDE.md` returns 0 (old system removed from docs)
    - `grep -c 'debug_log' CLAUDE.md` returns 0 (old field removed from docs)
    - `grep -c 'Apache' daydream/atif/NOTICE` returns >= 1 (license present)
    - `grep -c 'Harbor\|harbor\|Laude\|laude' daydream/atif/NOTICE` returns >= 1 (attribution present)
    - `grep -c '--trajectory' CLAUDE.md` returns >= 1 (new flag in examples)
  </acceptance_criteria>
  <done>CLAUDE.md updated with trajectory module reference, --debug removed, trajectory recording pattern documented. NOTICE file verified complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Documentation changes only -- no production code modifications, no new attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | I (Information Disclosure) | README.md | accept | README documents redaction policy publicly. The policy itself is not sensitive -- it describes what categories are scrubbed, not how to circumvent scrubbing. |
</threat_model>

<verification>
1. `grep '## Trajectory Output' README.md` -- section exists
2. `grep '\[0\.14\.0\]' CHANGELOG.md` -- version entry exists
3. `grep 'trajectory.py' CLAUDE.md` -- module referenced
4. `grep 'Apache' daydream/atif/NOTICE` -- attribution present
5. `grep -c '\-\-debug' README.md CLAUDE.md` -- returns 0 for both (old flag removed)
</verification>

<success_criteria>
- DOCS-01: README has Trajectory Output section with format overview, default path, --trajectory flag
- DOCS-02: README explains the 4 redaction categories with [REDACTED_*] tokens
- DOCS-03: README has 3 consumer integration bullets linking to Harbor
- DOCS-04: CHANGELOG has [0.14.0] with Breaking (--debug removed), Added (trajectory output, --trajectory flag, redaction, sibling files, SIGINT flush), Removed (_log_debug system, debug_log field, .review-debug-*.log)
- DOCS-05: NOTICE file has Apache-2.0 attribution for vendored Harbor code
- DOCS-06: CLAUDE.md mentions trajectory.py, TrajectoryRecorder, ContextVar propagation; no --debug or _log_debug references remain
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-hardening-documentation/05-03-SUMMARY.md`
</output>
