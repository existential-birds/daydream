---
phase: 02-pre-scan-exploration
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - daydream/exploration_runner.py
  - daydream/runner.py
  - daydream/phases.py
  - daydream/ui.py
  - daydream/backends/codex.py
  - tests/test_exploration_runner.py
  - tests/test_integration.py
  - tests/test_runner.py
autonomous: true
requirements:
  - AGNT-01
  - EXPL-01
  - EXPL-02
  - EXPL-03
  - EXPL-04
user_setup: []

must_haves:
  truths:
    - "Running `daydream --ttt` on a 4+ file diff populates RunConfig.exploration_context before phase_understand_intent is called"
    - "Running `daydream` (normal flow) on a 4+ file diff populates RunConfig.exploration_context before phase_review is called"
    - "The parallel tier launches exactly one Backend.execute() call with agents={'pattern-scanner', 'dependency-tracer', 'test-mapper'}"
    - "The single tier (2-3 files) launches one Backend.execute() call with agents={'dependency-tracer'} only"
    - "The skip tier (0-1 files) launches zero Backend.execute() calls and prints a dim 'Skipping exploration' notice"
    - "CodexBackend raises a clear NotImplementedError when called with agents=... (or cleanly refuses via a capability check)"
    - "Exploration runs inside safe_explore() -- a subagent crash leaves an empty ExplorationContext, not an exception bubbling up"
    - "RunConfig has an exploration_depth: int = 1 field"
  artifacts:
    - path: "daydream/exploration_runner.py"
      provides: "pre_scan(), tier dispatch, Backend.execute(agents=...) call, result parsing, merge"
      contains: "async def pre_scan"
      min_lines: 120
    - path: "daydream/runner.py"
      provides: "RunConfig.exploration_context and exploration_depth fields; pre_scan call in run() and run_trust()"
      contains: "exploration_context"
    - path: "daydream/ui.py"
      provides: "EXPLORE phase hero + per-subagent live panel row helpers"
      contains: "print_phase_hero"
    - path: "daydream/backends/codex.py"
      provides: "NotImplementedError on agents= kwarg"
      contains: "NotImplementedError"
  key_links:
    - from: "daydream/runner.py::run_trust"
      to: "daydream/exploration_runner.py::pre_scan"
      via: "safe_explore(pre_scan, ...) before phase_understand_intent"
      pattern: "safe_explore.*pre_scan"
    - from: "daydream/runner.py::run"
      to: "daydream/exploration_runner.py::pre_scan"
      via: "safe_explore(pre_scan, ...) before phase_review"
      pattern: "safe_explore.*pre_scan"
    - from: "daydream/exploration_runner.py::pre_scan"
      to: "daydream/backends/claude.py::execute"
      via: "backend.execute(agents=EXPLORATION_AGENTS_FOR_TIER)"
      pattern: "backend\\.execute\\(.*agents="
    - from: "daydream/exploration_runner.py::pre_scan"
      to: "daydream/exploration.py::merge_contexts"
      via: "merge_contexts(*partials)"
      pattern: "merge_contexts"
---

<objective>
Wire Plans 02 and 03 together: build the orchestrator that counts changed files, picks a tier, launches the right subagent set via `Backend.execute(agents=...)`, parses each subagent's structured-JSON result into a partial `ExplorationContext`, merges them, populates `RunConfig.exploration_context`, and plugs into both `run()` and `run_trust()`. Add the EXPLORE phase hero and live-panel UX. Make `CodexBackend` explicitly unsupported for exploration.

Purpose: Delivers the full Phase 2 goal from ROADMAP.md -- parallel subagents explore affected areas before review starts, for both TTT and normal flows, with graceful degradation.

Output: `daydream/exploration_runner.py` orchestrator, `RunConfig` fields, wiring in both `run()` and `run_trust()`, UX additions, CodexBackend guard, and end-to-end integration tests using MockBackend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pre-scan-exploration/02-CONTEXT.md
@.planning/phases/02-pre-scan-exploration/02-RESEARCH.md
@.planning/phases/02-pre-scan-exploration/02-01-PLAN.md
@.planning/phases/02-pre-scan-exploration/02-02-PLAN.md
@.planning/phases/02-pre-scan-exploration/02-03-PLAN.md
@daydream/exploration.py
@daydream/runner.py
@daydream/phases.py
@daydream/backends/__init__.py
@daydream/backends/claude.py
@daydream/backends/codex.py
@daydream/ui.py
@daydream/tree_sitter_index.py

<interfaces>
From Plan 02 (daydream/tree_sitter_index.py):

```python
def detect_affected_files(diff_text: str, repo_root: Path, depth: int = 1) -> list[FileInfo]
```

From Plan 03 (daydream/prompts/exploration_subagents.py):

```python
EXPLORATION_AGENTS: dict[str, AgentDefinition]  # keys: pattern-scanner, dependency-tracer, test-mapper
PATTERN_SCANNER_SCHEMA: dict[str, Any]
DEPENDENCY_TRACER_SCHEMA: dict[str, Any]
TEST_MAPPER_SCHEMA: dict[str, Any]
build_pattern_scanner_prompt(diff_text, affected_files) -> str
build_dependency_tracer_prompt(diff_text, affected_files) -> str
build_test_mapper_prompt(diff_text, affected_files) -> str
```

From Plan 03 (daydream/exploration.py):

```python
def merge_contexts(*contexts: ExplorationContext) -> ExplorationContext
```

From Phase 1 (daydream/backends/__init__.py, corrected in Plan 01):

```python
class Backend(Protocol):
    def execute(
        self,
        cwd: Path,
        prompt: str,
        output_schema: dict[str, Any] | None = None,
        continuation: ContinuationToken | None = None,
        agents: dict[str, AgentDefinition] | None = None,
    ) -> AsyncIterator[AgentEvent]: ...
```

From Phase 1 (daydream/exploration.py):

```python
async def safe_explore(explore_fn, *args, **kwargs) -> ExplorationContext
```

Existing `run_agent()` in `daydream/agent.py` wraps `Backend.execute()` and drains its event stream into a `(structured_output, continuation)` tuple. Reuse it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build daydream/exploration_runner.py with tier dispatch and subagent orchestration</name>
  <read_first>
    - daydream/exploration.py (ExplorationContext, FileInfo, merge_contexts, safe_explore)
    - daydream/prompts/exploration_subagents.py (EXPLORATION_AGENTS, schemas, builders)
    - daydream/tree_sitter_index.py (detect_affected_files)
    - daydream/agent.py (run_agent signature and return shape)
    - daydream/phases.py (how FEEDBACK_SCHEMA-style structured output is extracted from ResultMessage)
    - tests/test_exploration_runner.py (Wave 0 placeholders that this task unmasks)
    - tests/test_phases.py (find the MockBackend implementation to extend or mirror)
  </read_first>
  <files>daydream/exploration_runner.py, tests/test_exploration_runner.py</files>
  <behavior>
    - `count_changed_files(diff_text: str) -> int` returns the number of unique file paths in a unified diff.
    - `select_tier(file_count: int) -> Literal["skip", "single", "parallel"]` returns `"skip"` for 0-1, `"single"` for 2-3, `"parallel"` for 4+.
    - `async def pre_scan(backend: Backend, repo_root: Path, diff_text: str, depth: int = 1) -> ExplorationContext`:
      - If tier is "skip": returns empty `ExplorationContext` without calling backend.
      - If tier is "single": calls `backend.execute()` exactly once with `agents={"dependency-tracer": EXPLORATION_AGENTS["dependency-tracer"]}` and a lead prompt instructing delegation.
      - If tier is "parallel": calls `backend.execute()` exactly once with `agents=EXPLORATION_AGENTS` (all three keys) and a lead prompt instructing parallel delegation to all three.
    - The lead prompt tells the lead agent: "You have specialists named pattern-scanner, dependency-tracer, and test-mapper (or just dependency-tracer in single tier). Delegate to each specialist in parallel, then emit a single JSON object with the combined results matching this envelope schema: {pattern_scanner: PATTERN_SCANNER_SCHEMA, dependency_tracer: DEPENDENCY_TRACER_SCHEMA, test_mapper: TEST_MAPPER_SCHEMA}." The envelope schema is defined in the same module as `EXPLORATION_ENVELOPE_SCHEMA` and passed as `output_schema=` to `backend.execute()`.
    - `_parse_envelope(envelope: dict[str, Any]) -> ExplorationContext`: reads each sub-key, converts each list into the matching dataclass (FileInfo/Convention/Dependency), returns one `ExplorationContext`. Missing keys in the envelope (single-tier case) are treated as empty. Validate field types and skip entries that don't conform (graceful, no exception).
    - The pre-scan orchestrator also calls `detect_affected_files(diff_text, repo_root, depth)` up-front and merges those results into the final context alongside the subagent output via `merge_contexts(static_context, subagent_context)`. This is what delivers EXPL-01/EXPL-02 even if subagents fail.
  </behavior>
  <action>
    1. Create `daydream/exploration_runner.py`:
       - Module docstring: "Pre-scan orchestrator. Counts changed files, selects tier, launches specialist subagents via Backend.execute(agents=...), merges results into ExplorationContext."
       - `from __future__ import annotations` and standard imports.
       - `TYPE_CHECKING` guard for `Backend`, `AgentDefinition`.
    2. Implement `count_changed_files(diff_text: str) -> int`:
       - Scan for lines matching `^diff --git a/(.+) b/` and count unique paths. Use `re` (not `regex` lib) with a compiled pattern. This is parsing git's OWN output format, not source code, so a regex is acceptable -- document the distinction in a comment referencing D-04.
    3. Implement `select_tier(file_count: int) -> str`:
       - `<= 1`: return `"skip"`.
       - `2 or 3`: return `"single"`.
       - `>= 4`: return `"parallel"`.
       - Literal type alias at module top for clarity: `Tier: TypeAlias = Literal["skip", "single", "parallel"]`.
    4. Implement `EXPLORATION_ENVELOPE_SCHEMA` as a JSON Schema object combining the three Plan 03 schemas:
       ```python
       EXPLORATION_ENVELOPE_SCHEMA = {
           "type": "object",
           "properties": {
               "pattern_scanner": PATTERN_SCANNER_SCHEMA,
               "dependency_tracer": DEPENDENCY_TRACER_SCHEMA,
               "test_mapper": TEST_MAPPER_SCHEMA,
           },
           "required": [],  # any subset allowed -- single tier only emits one key
       }
       ```
    5. Implement `_build_lead_prompt(tier: str, diff_text: str, static_files: list[FileInfo]) -> str`:
       - For `"single"`: single paragraph instructing delegation to the `dependency-tracer` specialist only, with the diff and static file list embedded, closing with the envelope schema block.
       - For `"parallel"`: paragraph instructing parallel delegation to all three specialists, embedding the diff and static file list, closing with the envelope schema block.
       - Both include the dynamic per-specialist prompt segments by calling the Plan 03 builders and embedding them as `<pattern-scanner-instructions>...</pattern-scanner-instructions>` etc.
    6. Implement `_parse_envelope(envelope: dict[str, Any]) -> ExplorationContext`:
       - For each top-level key (`pattern_scanner`, `dependency_tracer`, `test_mapper`) that exists, pull out the field arrays and coerce into the matching dataclass. Use `FileInfo(**entry)` style with try/except on each entry -- skip malformed ones.
       - Return the merged `ExplorationContext` built from the coerced lists.
    7. Implement `async def pre_scan(backend, repo_root, diff_text, depth=1) -> ExplorationContext`:
       - `static_files = detect_affected_files(diff_text, repo_root, depth)`
       - `static_context = ExplorationContext(affected_files=static_files)`
       - `file_count = count_changed_files(diff_text)`
       - `tier = select_tier(file_count)`
       - If `tier == "skip"`: return `static_context` (or empty if no files).
       - Else, build the agents dict (single tier = one key, parallel tier = three keys), build the lead prompt, call `run_agent(backend=backend, cwd=repo_root, prompt=..., output_schema=EXPLORATION_ENVELOPE_SCHEMA, agents=agents)`.
       - `run_agent` returns `(structured_output, continuation_token)`. If `structured_output` is None (parse failure) log a warning and return `static_context` alone. Otherwise pass through `_parse_envelope` and merge.
       - Return `merge_contexts(static_context, subagent_context)`.
    8. Edit `tests/test_exploration_runner.py`: for each of the three orchestrator tests, replace the module-level importorskip (or per-function importorskip) with a real import of `daydream.exploration_runner`. Flesh out bodies:
       - `test_skip_tier_no_subagents`: load `trivial_single.diff`, instantiate a `MockBackend` (see step 9), call `anyio.run(pre_scan, mock, tmp_path, diff_text)`, assert `mock.agents_calls == []` and the returned context has `affected_files` either empty or one entry with `role="modified"`.
       - `test_single_tier_dependency_tracer_only`: build a 2-file diff string inline (or reuse a 2-file subset of python_multifile.diff), call pre_scan, assert `len(mock.agents_calls) == 1`, `set(mock.agents_calls[0].keys()) == {"dependency-tracer"}`.
       - `test_parallel_tier_launches_three_agents`: use python_multifile.diff but extend it to 4+ files by concatenating with typescript_multifile.diff, call pre_scan, assert `len(mock.agents_calls) == 1`, `set(mock.agents_calls[0].keys()) == {"pattern-scanner", "dependency-tracer", "test-mapper"}`.
    9. Extend the existing `MockBackend` in `tests/test_phases.py` (or wherever `MockBackendWithEvents` lives -- verify with grep) to record every `agents=` kwarg into `self.agents_calls: list[dict[str, Any]]`. If the existing MockBackend in tests is frozen by too many other tests, create a new `_AgentsRecordingMockBackend` class inside `tests/test_exploration_runner.py` instead -- it must:
       - Implement `async def execute(self, cwd, prompt, output_schema=None, continuation=None, agents=None) -> AsyncIterator[AgentEvent]`.
       - Append `dict(agents) if agents else None` to `self.agents_calls`.
       - Yield one `ResultEvent(structured_output={"pattern_scanner": {...}, "dependency_tracer": {...}, "test_mapper": {...}}, continuation=None)` with a canned empty-but-valid envelope so `_parse_envelope` has something to chew on.
       - Provide stub `cancel()` and `format_skill_invocation()` methods.
    10. Run `uv run pytest tests/test_exploration_runner.py -v`. All four tests (three orchestrator + one prompt, unmasked in Plan 03) pass.
    11. Run `uv run mypy daydream/exploration_runner.py` and `uv run ruff check daydream/exploration_runner.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_exploration_runner.py -v &amp;&amp; uv run mypy daydream/exploration_runner.py &amp;&amp; uv run ruff check daydream/exploration_runner.py</automated>
  </verify>
  <done>
    - `daydream/exploration_runner.py` exists with `pre_scan`, `count_changed_files`, `select_tier`, `EXPLORATION_ENVELOPE_SCHEMA`, `_parse_envelope`.
    - All four `tests/test_exploration_runner.py` tests pass.
    - mypy + ruff clean.
  </done>
  <acceptance_criteria>
    - `grep -c "async def pre_scan" daydream/exploration_runner.py` == 1.
    - `grep -c "def count_changed_files" daydream/exploration_runner.py` == 1.
    - `grep -c "def select_tier" daydream/exploration_runner.py` == 1.
    - `grep -c "EXPLORATION_ENVELOPE_SCHEMA" daydream/exploration_runner.py` >= 2.
    - `grep -c "merge_contexts" daydream/exploration_runner.py` >= 1.
    - `grep -c "detect_affected_files" daydream/exploration_runner.py` >= 1.
    - `grep -c "agents=" daydream/exploration_runner.py` >= 1.
    - `grep -c "importorskip" tests/test_exploration_runner.py` == 0.
    - `uv run pytest tests/test_exploration_runner.py -v` reports 4 passed, 0 failed, 0 skipped.
    - `uv run mypy daydream` exits 0.
    - `uv run ruff check daydream` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire pre_scan into runner.py (both run() and run_trust()), add RunConfig fields, add UX hero + panel, and guard CodexBackend</name>
  <read_first>
    - daydream/runner.py (entire file -- find `run()`, `run_trust()`, `RunConfig`, `_git_diff`)
    - daydream/phases.py (find `phase_review`, `phase_understand_intent` call sites)
    - daydream/ui.py (find `print_phase_hero`, `print_dim` or equivalent, `LiveToolPanelRegistry`)
    - daydream/backends/codex.py (find `execute` signature)
    - daydream/exploration.py (safe_explore signature)
    - daydream/exploration_runner.py (pre_scan signature from Task 1)
    - tests/test_integration.py and tests/test_runner.py (to see how MockBackend is injected and how RunConfig is built in tests)
  </read_first>
  <files>daydream/runner.py, daydream/phases.py, daydream/ui.py, daydream/backends/codex.py, tests/test_integration.py, tests/test_runner.py</files>
  <behavior>
    - `RunConfig` gains two fields: `exploration_context: ExplorationContext | None = None` and `exploration_depth: int = 1`.
    - `run()` calls `config.exploration_context = await safe_explore(pre_scan, backend, repo_root, diff_text, config.exploration_depth)` BEFORE the first `phase_review(...)` call.
    - `run_trust()` does the same BEFORE the first `phase_understand_intent(...)` call.
    - Both call sites are guarded: if a git diff can't be obtained (non-git repo, subprocess error), `safe_explore` returns an empty context and the flows continue (existing behavior).
    - When tier is "skip", the UI prints a dim "Skipping exploration -- trivial diff" message via a new `print_dim` helper (or existing equivalent) instead of the phase hero.
    - When tier is "single" or "parallel", `print_phase_hero("EXPLORE")` fires before `pre_scan` runs.
    - `CodexBackend.execute()` raises `NotImplementedError("Codex backend does not support exploration subagents; use --backend claude for exploration.")` when `agents is not None`.
    - New integration test `test_run_populates_exploration_context` in `tests/test_integration.py` constructs a `RunConfig` with a MockBackend (or patches `create_backend`) and a 4-file diff fixture, runs the async `run()` function, and asserts `config.exploration_context is not None` before `phase_review` is invoked. Use the same patching pattern already used by other integration tests in that file.
    - New test `test_run_config_exploration_depth` in `tests/test_runner.py` asserts `RunConfig().exploration_depth == 1` and that passing `exploration_depth=2` sets the field.
  </behavior>
  <action>
    1. **RunConfig changes** (`daydream/runner.py`):
       - Add `from daydream.exploration import ExplorationContext` at the top of the file.
       - Add two fields to the `RunConfig` dataclass: `exploration_context: ExplorationContext | None = None` and `exploration_depth: int = 1`. Update the `Attributes:` docstring section.
    2. **run() wiring** (`daydream/runner.py`):
       - Inside `run(config)`, locate where the diff is obtained for review. If `run()` does not currently call `_git_diff` (it may only do so inside `run_trust`), add an early call using the existing `_git_diff(config.target_dir)` helper. Wrap it so a non-git repo yields empty string.
       - Before the first `phase_review(...)` call, add:
         ```python
         from daydream.exploration import safe_explore
         from daydream.exploration_runner import pre_scan, count_changed_files, select_tier
         from daydream.ui import print_phase_hero, print_dim
         tier = select_tier(count_changed_files(diff_text))
         if tier == "skip":
             print_dim(console, "Skipping exploration -- trivial diff")
             config.exploration_context = ExplorationContext()
         else:
             print_phase_hero(console, "EXPLORE")
             config.exploration_context = await safe_explore(
                 pre_scan, backend, config.target_dir, diff_text, config.exploration_depth,
             )
         ```
       - Use the backend resolved via `_resolve_backend(config, "review")` or the existing review backend -- whatever the current code does for the review phase. Exploration reuses the review backend (no new per-phase override in this phase).
    3. **run_trust() wiring** (`daydream/runner.py`):
       - Same treatment, inserted immediately before the first `phase_understand_intent(...)` call. Use the same imports and the trust-path backend.
    4. **phases.py consumption hooks** (`daydream/phases.py`):
       - `phase_review`, `phase_understand_intent`, `phase_alternative_review`, `phase_generate_plan` each gain an optional `exploration_context: ExplorationContext | None = None` parameter (keyword-only, defaulting to None for backward compat).
       - Inside each of these phase functions, if `exploration_context is not None and exploration_context.to_prompt_section()` is non-empty, prepend the prompt-section to the existing prompt text. If None or empty, behavior is unchanged.
       - Update the call sites in `runner.py` to pass `config.exploration_context` to each of these four phase functions.
       - NOTE: deeper consumption (confidence scoring, convention filtering, grounded recommendations) is Phase 3. This task only injects the to_prompt_section text -- nothing more.
    5. **UI additions** (`daydream/ui.py`):
       - If `print_phase_hero(console, name)` already accepts any phase name, just confirm it works with `"EXPLORE"` and add no new code. If it has a hardcoded list, add `"EXPLORE"` to the set.
       - If `print_dim` does not exist, add a helper: `def print_dim(console: Console, message: str) -> None:` that prints `message` in Rich `style="dim"`. Mirror the existing `print_info` / `print_warning` pattern. Export it.
       - Per D-15, a dedicated `LiveToolPanelRegistry`-style per-subagent panel is nice-to-have but NOT a blocker for this task. Use the existing `LiveToolPanelRegistry` only if it can be driven from the `agent.run_agent()` event stream without modification. If that requires changes to `run_agent()`, skip the live panel in this plan and leave the default streaming text output -- document this in the SUMMARY. The phase hero alone is sufficient to mark the EXPLORE phase visually.
    6. **Codex guard** (`daydream/backends/codex.py`):
       - In `CodexBackend.execute()`, add `agents: dict[str, AgentDefinition] | None = None` to the signature (mirroring the Protocol).
       - At the top of the function body, add:
         ```python
         if agents is not None:
             raise NotImplementedError(
                 "Codex backend does not support exploration subagents; use --backend claude for exploration."
             )
         ```
       - Import `AgentDefinition` under `TYPE_CHECKING`.
    7. **Tests**:
       - `tests/test_runner.py`: add `test_run_config_exploration_depth` asserting default and override behavior of the new field. Add `test_run_config_exploration_context_defaults_to_none`.
       - `tests/test_integration.py`: add `test_run_populates_exploration_context` that:
         - Monkeypatches `create_backend` in the runner module to return an `_AgentsRecordingMockBackend` (import from `tests/test_exploration_runner.py` or duplicate locally) that yields a valid envelope.
         - Builds a synthetic 4-file diff by concatenating `python_multifile.diff` and `typescript_multifile.diff`.
         - Monkeypatches `_git_diff` to return that concatenated diff string.
         - Runs `anyio.run(run, config)` with a stub `phase_review` (monkeypatched to a no-op that captures `config.exploration_context` at the time of call).
         - Asserts the captured exploration_context is not None and has at least the affected_files from `detect_affected_files`.
       - `tests/test_integration.py`: add `test_codex_backend_raises_on_agents` that instantiates a `CodexBackend` directly and calls `execute(agents={"x": ...})`, asserting `NotImplementedError` is raised. Use `pytest.raises`.
    8. Run `make check`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_runner.py tests/test_integration.py tests/test_exploration_runner.py -v &amp;&amp; make check</automated>
  </verify>
  <done>
    - `RunConfig.exploration_context` and `exploration_context.exploration_depth` fields exist.
    - Both `run()` and `run_trust()` populate `config.exploration_context` via `safe_explore(pre_scan, ...)` before their first phase call.
    - Four phase functions accept and inject `exploration_context` via `to_prompt_section()`.
    - `CodexBackend.execute(agents=...)` raises NotImplementedError.
    - `print_dim` helper exists and is used for the skip-tier message.
    - EXPLORE hero fires for single/parallel tiers.
    - All new tests pass.
    - `make check` is green.
  </done>
  <acceptance_criteria>
    - `grep -c "exploration_context: ExplorationContext | None" daydream/runner.py` >= 1.
    - `grep -c "exploration_depth: int = 1" daydream/runner.py` >= 1.
    - `grep -c "safe_explore" daydream/runner.py` >= 2 (once for run, once for run_trust).
    - `grep -c "pre_scan" daydream/runner.py` >= 2.
    - `grep -c "print_phase_hero.*EXPLORE\\|\"EXPLORE\"" daydream/runner.py` >= 2.
    - `grep -c "exploration_context" daydream/phases.py` >= 4 (one per updated phase function).
    - `grep -c "to_prompt_section" daydream/phases.py` >= 1.
    - `grep -c "NotImplementedError" daydream/backends/codex.py` >= 1.
    - `grep -c "Codex backend does not support exploration" daydream/backends/codex.py` == 1.
    - `grep -c "def print_dim\\|print_dim" daydream/ui.py` >= 1.
    - `grep -c "test_run_config_exploration_depth\\|test_run_config_exploration_context_defaults_to_none" tests/test_runner.py` == 2.
    - `grep -c "test_run_populates_exploration_context\\|test_codex_backend_raises_on_agents" tests/test_integration.py` == 2.
    - `uv run pytest -v` exits 0.
    - `make check` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- AGNT-01: parallel tier test passes with exactly the three named specialists.
- EXPL-01, EXPL-02: detect_affected_files feeds static context into pre_scan, merged with subagent output.
- EXPL-03, EXPL-04: pattern-scanner runs as part of the parallel tier with CLAUDE.md/.coderabbit.yaml instructions.
- Both flows (normal and --ttt) populate RunConfig.exploration_context before their first phase call.
- Codex backend cleanly refuses exploration.
- Full suite green; `make check` passes.
</verification>

<success_criteria>
- All five Phase 2 success criteria from ROADMAP.md hold:
  1. `daydream --ttt` on a multi-file diff produces an ExplorationContext with file maps, conventions, dependencies before review.
  2. Three parallel subagents each complete and return structured results (via MockBackend for now; live verification is manual per VALIDATION.md).
  3. Exploration subagents read project guidelines -- enforced by the pattern-scanner prompt constants.
  4. `detect_affected_files()` correctly identifies changed files and their immediate imports/callers (Plan 02 + this task's static merge).
  5. Exploration scales with diff size -- skip / single / parallel tiers verified in unit tests.
- `make check` passes.
- No regressions in the 50+ existing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pre-scan-exploration/02-04-SUMMARY.md`.
</output>
