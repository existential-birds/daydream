---
phase: 02-pre-scan-exploration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - daydream/backends/__init__.py
  - daydream/backends/claude.py
  - tests/test_backend_claude.py
  - tests/test_exploration_runner.py
  - tests/test_tree_sitter_index.py
  - tests/test_exploration.py
  - tests/fixtures/diffs/python_multifile.diff
  - tests/fixtures/diffs/typescript_multifile.diff
  - tests/fixtures/diffs/go_multifile.diff
  - tests/fixtures/diffs/rust_multifile.diff
  - tests/fixtures/diffs/trivial_single.diff
  - pyproject.toml
autonomous: true
requirements:
  - AGNT-01
user_setup: []

must_haves:
  truths:
    - "Backend.execute(agents=...) accepts dict[str, AgentDefinition] (named specialists, not anonymous list)"
    - "ClaudeBackend forwards the agents dict to ClaudeAgentOptions verbatim, preserving the caller-supplied keys (pattern-scanner, dependency-tracer, test-mapper)"
    - "Test scaffolding files exist for tree_sitter_index, exploration_runner, and the backend agents-shape smoke test, each with at least one xfail/placeholder test that pytest collects"
    - "tree-sitter and the four grammar packages are pinned in pyproject.toml and resolved in uv.lock"
    - "Diff fixtures for python/typescript/go/rust/trivial cases exist on disk under tests/fixtures/diffs/"
  artifacts:
    - path: "daydream/backends/__init__.py"
      provides: "Backend protocol with agents: dict[str, AgentDefinition] | None"
      contains: "dict[str, AgentDefinition]"
    - path: "daydream/backends/claude.py"
      provides: "ClaudeBackend.execute() forwarding agents dict unchanged"
      contains: "options.agents = agents"
    - path: "tests/test_backend_claude.py"
      provides: "Smoke test asserting agents dict reaches ClaudeAgentOptions with original keys"
      contains: "test_execute_passes_agents_dict_to_options"
    - path: "tests/test_tree_sitter_index.py"
      provides: "Test scaffolding for detect_affected_files()"
      contains: "test_python_impact_surface"
    - path: "tests/test_exploration_runner.py"
      provides: "Test scaffolding for tier dispatch + subagent prompt assertions"
      contains: "test_parallel_tier_launches_three_agents"
    - path: "pyproject.toml"
      provides: "tree-sitter dependencies pinned"
      contains: "tree-sitter-python"
  key_links:
    - from: "daydream/backends/claude.py"
      to: "ClaudeAgentOptions.agents"
      via: "options.agents = agents (no key rewriting)"
      pattern: "options\\.agents = agents"
---

<objective>
Wave 0: Resolve the Phase 1 protocol/SDK shape mismatch, install tree-sitter dependencies, and create empty-but-collected test scaffolds and diff fixtures so every downstream task in Waves 1-2 has a real `<automated>` command to run. No production logic in this plan beyond the protocol fix.

Purpose: Phase 1 declared `agents: list[AgentDefinition]` and ClaudeBackend re-keys it to `explorer-{i}`, throwing away the specialist names the SDK uses to dispatch subagents. Phase 2's orchestrator MUST be able to pass `{"pattern-scanner": ..., "dependency-tracer": ..., "test-mapper": ...}` and have those exact names survive into `ClaudeAgentOptions`. Fix this once, here, before any orchestrator code is built on top.

Output: Corrected Backend protocol + ClaudeBackend, pinned tree-sitter deps, and the test scaffolding (files + fixtures) that Waves 1-2 fill in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pre-scan-exploration/02-CONTEXT.md
@.planning/phases/02-pre-scan-exploration/02-RESEARCH.md
@.planning/phases/02-pre-scan-exploration/02-VALIDATION.md
@daydream/backends/__init__.py
@daydream/backends/claude.py
@daydream/exploration.py

<interfaces>
Current (wrong) Phase 1 shape in daydream/backends/__init__.py line 93:

```python
agents: list[AgentDefinition] | None = None,
```

Current (wrong) Phase 1 shape in daydream/backends/claude.py lines 50, 80-81:

```python
agents: list[AgentDefinition] | None = None,
...
if agents:
    options.agents = {f"explorer-{i}": a for i, a in enumerate(agents)}
```

Target SDK shape (verified in .claude/skills/claude-agent-sdk/REFERENCE.md line 57):

```python
ClaudeAgentOptions(agents: dict[str, AgentDefinition] | None = None)
```

`AgentDefinition` (REFERENCE.md line 297) has fields `description`, `prompt`, `tools`, `model` -- NO `name` field. The specialist name lives in the dict key.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Backend protocol + ClaudeBackend to use dict[str, AgentDefinition] and add smoke test</name>
  <read_first>
    - daydream/backends/__init__.py (entire file)
    - daydream/backends/claude.py (entire file)
    - tests/test_backend_claude.py (entire file -- this exists from Phase 1; extend it, do not replace)
    - .planning/phases/02-pre-scan-exploration/02-RESEARCH.md sections "CRITICAL FOUNDATION DISCREPANCY" and "Pitfall 3"
  </read_first>
  <files>daydream/backends/__init__.py, daydream/backends/claude.py, tests/test_backend_claude.py</files>
  <behavior>
    - Test 1 (smoke, RED first): `test_execute_passes_agents_dict_to_options` -- monkeypatches `ClaudeSDKClient` and `ClaudeAgentOptions` so the test can capture the kwargs the backend constructs. Calls `ClaudeBackend().execute(cwd=..., prompt="x", agents={"pattern-scanner": <fake AgentDefinition>, "dependency-tracer": <fake>})`, drains the async iterator, and asserts that the captured `ClaudeAgentOptions` has `agents == {"pattern-scanner": <fake>, "dependency-tracer": <fake>}` -- exact same keys, no `explorer-0` rewriting.
    - Test 2: `test_execute_passes_none_when_no_agents` -- same harness, called without `agents=`; asserts `options.agents` is either unset or `None`.
    - Test 3: `test_backend_protocol_agents_param_is_dict_typed` -- inspects `Backend.execute.__annotations__["agents"]` (or uses `typing.get_type_hints` with `include_extras=True`) and asserts the annotation string contains `dict[str, AgentDefinition]`.
  </behavior>
  <action>
    1. Edit daydream/backends/__init__.py: change line 93 from `agents: list[AgentDefinition] | None = None,` to `agents: dict[str, AgentDefinition] | None = None,`. Leave the `TYPE_CHECKING` import block alone.
    2. Edit daydream/backends/claude.py:
       - Line 50: change `agents: list[AgentDefinition] | None = None,` to `agents: dict[str, AgentDefinition] | None = None,`
       - Lines 80-81: replace
         ```
         if agents:
             options.agents = {f"explorer-{i}": a for i, a in enumerate(agents)}
         ```
         with
         ```
         if agents:
             options.agents = agents
         ```
       - Update the docstring on line 59 from "Optional list of AgentDefinition for subagent support." to "Optional mapping of specialist name -> AgentDefinition for subagent support. Keys are the specialist names the lead agent dispatches by; they MUST be preserved verbatim."
    3. Extend tests/test_backend_claude.py with the three tests above. Use the existing monkeypatch pattern in that file (it already mocks `ClaudeSDKClient` -- mirror it). Build the fake `AgentDefinition` as a `SimpleNamespace` or a real `AgentDefinition` instance imported under `TYPE_CHECKING`-safe conditions.
    4. Run `uv run pytest tests/test_backend_claude.py -x` and confirm all three new tests pass alongside existing ones.
    5. Run `uv run mypy daydream` -- the protocol change must not introduce type errors.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backend_claude.py::test_execute_passes_agents_dict_to_options tests/test_backend_claude.py::test_execute_passes_none_when_no_agents tests/test_backend_claude.py::test_backend_protocol_agents_param_is_dict_typed -x &amp;&amp; uv run mypy daydream</automated>
  </verify>
  <done>
    - `grep -n "dict\\[str, AgentDefinition\\]" daydream/backends/__init__.py daydream/backends/claude.py` returns matches in both files.
    - `grep -n "explorer-{i}" daydream/backends/claude.py` returns NOTHING.
    - `grep -n "options.agents = agents" daydream/backends/claude.py` returns exactly one match.
    - All three new tests in tests/test_backend_claude.py pass.
    - `uv run mypy daydream` exits 0.
    - `uv run pytest -x` (full suite) stays green -- 50+ existing tests do not regress.
  </done>
  <acceptance_criteria>
    - `grep -c "list\\[AgentDefinition\\]" daydream/backends/__init__.py daydream/backends/claude.py` returns 0 for both files.
    - `grep -c "dict\\[str, AgentDefinition\\]" daydream/backends/__init__.py` >= 1.
    - `grep -c "dict\\[str, AgentDefinition\\]" daydream/backends/claude.py` >= 1.
    - `grep -c "options.agents = agents" daydream/backends/claude.py` == 1.
    - `grep -c "explorer-" daydream/backends/claude.py` == 0.
    - `uv run pytest tests/test_backend_claude.py -x` exits 0.
    - `uv run mypy daydream` exits 0.
    - `uv run pytest -x` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install tree-sitter dependencies and create test scaffolds + diff fixtures</name>
  <read_first>
    - pyproject.toml (entire [project] section)
    - .planning/phases/02-pre-scan-exploration/02-RESEARCH.md section "Standard Stack" and "Wave 0 Gaps"
    - .planning/phases/02-pre-scan-exploration/02-VALIDATION.md (the per-task verification map)
    - tests/test_exploration.py (existing Phase 1 tests -- mirror their pytest style)
  </read_first>
  <files>pyproject.toml, tests/test_tree_sitter_index.py, tests/test_exploration_runner.py, tests/test_exploration.py, tests/fixtures/diffs/python_multifile.diff, tests/fixtures/diffs/typescript_multifile.diff, tests/fixtures/diffs/go_multifile.diff, tests/fixtures/diffs/rust_multifile.diff, tests/fixtures/diffs/trivial_single.diff</files>
  <action>
    1. Run `uv add tree-sitter==0.25.2 tree-sitter-python==0.25.0 tree-sitter-typescript==0.23.2 tree-sitter-go==0.25.0 tree-sitter-rust==0.24.2`. This updates pyproject.toml and uv.lock together. If exact-version pins fail to resolve, fall back to `>=` floors using the same minimum versions and re-run.
    2. Smoke-import the grammars to catch ABI 14/15 mismatches early (Pitfall 1):
       ```
       uv run python -c "import tree_sitter_python, tree_sitter_typescript, tree_sitter_go, tree_sitter_rust; from tree_sitter import Language; Language(tree_sitter_python.language()); Language(tree_sitter_typescript.language_typescript()); Language(tree_sitter_typescript.language_tsx()); Language(tree_sitter_go.language()); Language(tree_sitter_rust.language()); print('OK')"
       ```
       Must print `OK`. If any grammar raises `Incompatible Language version`, find the matching grammar version newer than tree-sitter 0.25.0 release and pin to it.
    3. Create `tests/fixtures/diffs/` directory with five fixture files. Each is a real, parseable unified diff (use `git diff --name-status`-compatible format with `diff --git`, `index`, `---`, `+++`, and at least one hunk per file). Concrete contents:

       **python_multifile.diff** -- modifies two .py files. File A `daydream_demo/api.py` adds `from .models import User` and a function `def get_user(): return User()`. File B `daydream_demo/models.py` adds `class User: pass`. Both must have valid hunk headers.

       **typescript_multifile.diff** -- modifies `src/api.ts` (adds `import { User } from "./models";` and `export function getUser(): User { return new User(); }`) and `src/models.ts` (adds `export class User {}`).

       **go_multifile.diff** -- modifies `api.go` (adds `import "example.com/m/models"` and `func GetUser() *models.User { return &models.User{} }`) and `models/user.go` (adds `package models` and `type User struct{}`).

       **rust_multifile.diff** -- modifies `src/api.rs` (adds `use crate::models::User;` and `pub fn get_user() -> User { User }`) and `src/models.rs` (adds `pub struct User;`).

       **trivial_single.diff** -- modifies exactly one file `README.md` adding a single line. This is the "skip tier" fixture (1 changed file).

       File contents must be byte-exact diffs that `git apply --check` would accept against an empty repo with the target files pre-existing. Do not include binary markers, do not include rename detection. Keep each fixture under 60 lines.

    4. Create `tests/test_tree_sitter_index.py` with the following placeholder tests, each marked with `pytest.mark.xfail(reason="Wave 1: detect_affected_files() not implemented yet", strict=True)` so they collect, run, and fail in a controlled way until Wave 1 unmasks them by removing the marker:
       - `test_python_impact_surface` -- loads python_multifile.diff, calls `detect_affected_files(diff_text, repo_root, depth=1)`, asserts at least two FileInfo entries.
       - `test_typescript_impact_surface` -- same shape, typescript fixture.
       - `test_go_impact_surface` -- same shape, go fixture.
       - `test_rust_impact_surface` -- same shape, rust fixture.
       - `test_default_depth_is_one` -- asserts default depth parameter is 1.
       - `test_unsupported_language_gets_modified_role` -- diff containing a `.rb` file gets `role="modified"` with no dependencies traced.
       - `test_deleted_file_does_not_raise_filenotfound` -- diff with a `D` status entry (Pitfall 4); function returns the file with `role="modified"` and skips the import parse.

       Import the function from `daydream.tree_sitter_index` -- the import will fail today, so wrap it in a try/except ImportError and skip the entire module via `pytest.importorskip("daydream.tree_sitter_index")` at the top so Wave 0's pytest run does not error on collection. Wave 1 removes the importorskip when the module exists.

    5. Create `tests/test_exploration_runner.py` with the following placeholder tests, each using `pytest.importorskip("daydream.exploration_runner")` at module top:
       - `test_skip_tier_no_subagents` -- 1-file diff (trivial_single.diff): tier function returns "skip", orchestrator returns empty `ExplorationContext`, no agents launched.
       - `test_single_tier_dependency_tracer_only` -- 2-file diff: tier function returns "single", MockBackend records exactly one `agents` dict containing only `"dependency-tracer"`.
       - `test_parallel_tier_launches_three_agents` -- 4+ file diff: tier function returns "parallel", MockBackend records an `agents` dict with exactly the keys `{"pattern-scanner", "dependency-tracer", "test-mapper"}`.
       - `test_pattern_scanner_prompt_includes_guideline_files` -- imports the prompt builder and asserts the prompt string contains both `"CLAUDE.md"` and `".coderabbit.yaml"`.

    6. Extend `tests/test_exploration.py` with one new test (no importorskip needed -- exploration.py exists from Phase 1):
       - `test_merge_pattern_scanner_result` -- xfail until Wave 1 implements `merge_contexts()`. Asserts that merging a partial context with `conventions=[Convention(...)]` into an empty context produces a context whose `conventions` field has length 1.

    7. Run `uv run pytest tests/test_tree_sitter_index.py tests/test_exploration_runner.py tests/test_exploration.py -v` and confirm: tree_sitter_index and exploration_runner files are collected and SKIPPED (via importorskip), exploration.py tests run with the new xfail marked.

    8. Run `uv run pytest -v` (full suite) and confirm zero regressions.
  </action>
  <verify>
    <automated>uv run python -c "import tree_sitter_python, tree_sitter_typescript, tree_sitter_go, tree_sitter_rust; from tree_sitter import Language; Language(tree_sitter_python.language()); Language(tree_sitter_typescript.language_typescript()); Language(tree_sitter_typescript.language_tsx()); Language(tree_sitter_go.language()); Language(tree_sitter_rust.language()); print('OK')" &amp;&amp; uv run pytest tests/test_tree_sitter_index.py tests/test_exploration_runner.py tests/test_exploration.py -v &amp;&amp; uv run pytest -v</automated>
  </verify>
  <done>
    - `pyproject.toml` lists tree-sitter, tree-sitter-python, tree-sitter-typescript, tree-sitter-go, tree-sitter-rust under `[project].dependencies`.
    - `uv.lock` contains entries for all five.
    - The five-line smoke-import command above prints `OK`.
    - All five diff fixture files exist and are non-empty.
    - `tests/test_tree_sitter_index.py` and `tests/test_exploration_runner.py` collect cleanly under pytest (skipped via importorskip).
    - `tests/test_exploration.py::test_merge_pattern_scanner_result` is registered as xfail.
    - `uv run pytest -v` exits 0.
  </done>
  <acceptance_criteria>
    - `grep -c "tree-sitter-python" pyproject.toml` >= 1.
    - `grep -c "tree-sitter-typescript" pyproject.toml` >= 1.
    - `grep -c "tree-sitter-go" pyproject.toml` >= 1.
    - `grep -c "tree-sitter-rust" pyproject.toml` >= 1.
    - `ls tests/fixtures/diffs/python_multifile.diff tests/fixtures/diffs/typescript_multifile.diff tests/fixtures/diffs/go_multifile.diff tests/fixtures/diffs/rust_multifile.diff tests/fixtures/diffs/trivial_single.diff` lists all five.
    - Each fixture file has byte size > 200.
    - `grep -c "importorskip.*tree_sitter_index" tests/test_tree_sitter_index.py` >= 1.
    - `grep -c "importorskip.*exploration_runner" tests/test_exploration_runner.py` >= 1.
    - `grep -c "test_parallel_tier_launches_three_agents" tests/test_exploration_runner.py` == 1.
    - `grep -c "test_pattern_scanner_prompt_includes_guideline_files" tests/test_exploration_runner.py` == 1.
    - `grep -c "test_python_impact_surface\\|test_typescript_impact_surface\\|test_go_impact_surface\\|test_rust_impact_surface" tests/test_tree_sitter_index.py` == 4.
    - `uv run pytest -v` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All Phase 1 tests still pass.
- The agents-shape smoke test asserts named keys survive into ClaudeAgentOptions.
- Test scaffolding is in place so every Wave 1/2 task has a real `<automated>` command to point at.
- tree-sitter dependencies install on a fresh `uv sync` without compiler errors.
</verification>

<success_criteria>
- Backend protocol uses `dict[str, AgentDefinition]` end-to-end.
- ClaudeBackend forwards the dict verbatim with no key rewriting.
- tree-sitter + four grammars resolved in uv.lock and importable.
- Five diff fixtures exist on disk.
- Three test files (`test_tree_sitter_index.py`, `test_exploration_runner.py`, the extended `test_exploration.py`) collect cleanly and contain the placeholder/xfail tests Wave 1 will unmask.
- `make check` (lint + typecheck + full pytest) passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pre-scan-exploration/02-01-SUMMARY.md`.
</output>
