---
phase: 02-pre-scan-exploration
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - daydream/tree_sitter_index.py
  - tests/test_tree_sitter_index.py
autonomous: true
requirements:
  - EXPL-01
  - EXPL-02
user_setup: []

must_haves:
  truths:
    - "detect_affected_files() returns FileInfo entries for every changed file in a multi-file diff for Python, TypeScript, Go, and Rust"
    - "1-hop dependency tracing finds direct importers/imports for each supported language"
    - "Default trace depth is 1; deeper traces require an explicit depth argument"
    - "Files in unsupported languages are returned with role='modified' and no dependency tracing (no exception raised)"
    - "Deleted files (status D) do not raise FileNotFoundError"
    - "Adding a new language grammar in the future requires only a single LANGUAGES dict entry plus a query string -- no refactor"
  artifacts:
    - path: "daydream/tree_sitter_index.py"
      provides: "detect_affected_files(), LANGUAGES registry, parser cache, per-language import queries"
      contains: "def detect_affected_files"
      min_lines: 150
    - path: "tests/test_tree_sitter_index.py"
      provides: "Unit tests for impact surface, depth, unsupported languages, deleted files"
      contains: "test_python_impact_surface"
  key_links:
    - from: "daydream/tree_sitter_index.py"
      to: "tree_sitter_python / tree_sitter_typescript / tree_sitter_go / tree_sitter_rust"
      via: "lazy factory functions in LANGUAGES dict"
      pattern: "tree_sitter_(python|typescript|go|rust)"
    - from: "daydream/tree_sitter_index.py"
      to: "daydream/exploration.FileInfo"
      via: "import + return type"
      pattern: "from daydream.exploration import FileInfo"
---

<objective>
Build the pure, synchronous, tree-sitter-backed `detect_affected_files()` function that turns a git diff into the impact surface (changed files + 1-hop imports/importers) for Python, TypeScript/TSX/JavaScript, Go, and Rust. No async, no Backend, no UI -- this module is trivially unit-testable with the diff fixtures created in Wave 0.

Purpose: Satisfies EXPL-01 (impact surface) and EXPL-02 (immediate imports/callers) at the static-analysis layer. Phase 2's orchestrator (Plan 04) calls this function before launching subagents so the dependency-tracer subagent has a starting point and the pattern-scanner subagent has a list of files to scan.

Output: `daydream/tree_sitter_index.py` with the LANGUAGES registry, parser cache, per-language tree-sitter Query strings, and a synchronous `detect_affected_files()` function. All Wave 0 placeholder tests in `tests/test_tree_sitter_index.py` are unmasked and passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pre-scan-exploration/02-CONTEXT.md
@.planning/phases/02-pre-scan-exploration/02-RESEARCH.md
@daydream/exploration.py
@.planning/phases/02-pre-scan-exploration/02-01-PLAN.md

<interfaces>
From daydream/exploration.py:

```python
@dataclass
class FileInfo:
    path: str
    role: str           # "modified", "imported_by", "imports", "test"
    summary: str = ""
```

Target function signature:

```python
def detect_affected_files(
    diff_text: str,
    repo_root: Path,
    depth: int = 1,
) -> list[FileInfo]:
    """Return changed files plus their 1-hop import dependencies."""
```

LANGUAGES registry shape (from RESEARCH.md Pattern 1):

```python
LANGUAGES: dict[str, tuple[str, Callable[[], Language]]] = {
    ".py":  ("python",     _python_lang),
    ".ts":  ("typescript", _typescript_lang),
    ".tsx": ("tsx",        _tsx_lang),
    ".js":  ("javascript", _typescript_lang),
    ".jsx": ("tsx",        _tsx_lang),
    ".go":  ("go",         _go_lang),
    ".rs":  ("rust",       _rust_lang),
}
```

Per-language Query strings (RESEARCH.md Pattern 2 -- Python example shown; planner derives the other three from each grammar's node names):

```
PYTHON_IMPORT_QUERY = """
(import_statement name: (dotted_name) @import)
(import_from_statement module_name: (dotted_name) @import)
(import_from_statement module_name: (relative_import) @import)
"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build LANGUAGES registry, parser cache, and per-language import queries</name>
  <read_first>
    - .planning/phases/02-pre-scan-exploration/02-RESEARCH.md sections "Pattern 1: Lazy Language Registry" and "Pattern 2: Tree-sitter Query for Imports"
    - daydream/exploration.py (FileInfo dataclass)
    - tests/test_tree_sitter_index.py (the Wave 0 placeholders -- this task will unmask them)
    - For each grammar's import node names, fetch from the official grammar repos as needed (tree-sitter-python, tree-sitter-typescript, tree-sitter-go, tree-sitter-rust). Confirm node names by writing a tiny scratch script that parses a one-line import in each language and prints `tree.root_node.sexp()`.
  </read_first>
  <files>daydream/tree_sitter_index.py</files>
  <behavior>
    - When `get_parser("python")` is called twice, the second call returns the cached parser instance (identity check).
    - `LANGUAGES[".py"]` returns `("python", _python_lang)`.
    - `LANGUAGES[".tsx"]` and `LANGUAGES[".jsx"]` both map to language id `"tsx"`.
    - `LANGUAGES[".js"]` maps to language id `"javascript"` whose factory returns the typescript grammar (TS grammar handles JS per Pitfall 2).
    - `extract_imports(parser, source_bytes, query_string)` returns a list of decoded import strings for at least one valid Python source string `b"import os\\nfrom collections import abc\\n"` -- result contains `"os"` and `"collections"`.
  </behavior>
  <action>
    1. Create `daydream/tree_sitter_index.py` with module docstring "Tree-sitter-backed static import resolution for the exploration phase."
    2. Add `from __future__ import annotations` and Google-style imports grouped per CONVENTIONS.md.
    3. Implement the lazy factory functions `_python_lang`, `_typescript_lang`, `_tsx_lang`, `_go_lang`, `_rust_lang` exactly as shown in RESEARCH.md Pattern 1. Each function imports its grammar package inside the function body (lazy load).
    4. Define the module-level `LANGUAGES` dict mapping extensions to `(language_id, factory)` tuples per the interface block above.
    5. Define `_PARSER_CACHE: dict[str, Parser] = {}` and `get_parser(language_id: str) -> Parser | None` that constructs+caches `Parser(factory())` on first call. If `language_id` is not in any LANGUAGES value, return None.
    6. Define four query strings as module constants:
       - `PYTHON_IMPORT_QUERY` -- as shown in RESEARCH.md Pattern 2.
       - `TYPESCRIPT_IMPORT_QUERY` -- captures `(import_statement source: (string) @import)` and `(call_expression function: (identifier) @fn (#eq? @fn "require") arguments: (arguments (string) @import))`. Verify node names against tree-sitter-typescript grammar.
       - `GO_IMPORT_QUERY` -- captures `(import_spec path: (interpreted_string_literal) @import)` (Go imports are always strings). Verify against tree-sitter-go grammar.
       - `RUST_IMPORT_QUERY` -- captures `(use_declaration argument: (_) @import)`. Verify against tree-sitter-rust grammar.
    7. Implement helper `extract_imports(parser: Parser, source: bytes, query_string: str) -> list[str]`:
       - Parse source.
       - Run `Query(parser.language, query_string)` against the root node.
       - Return decoded `node.text.decode("utf-8")` for every captured node, stripped of surrounding quotes for TS/Go (call `.strip('\"\\'')`).
       - Wrap in try/except `Exception` -- on parse failure return `[]` (graceful degradation per D-06).
    8. Implement `_query_for_language(language_id: str) -> str | None` returning the right query constant for each id, None for unsupported.
    9. Add a smoke test inline at the bottom of the file under `if __name__ == "__main__":` that parses one import string per language and prints results -- useful for manual verification, removed during cleanup if it bloats the file.
    10. Run `uv run python -m daydream.tree_sitter_index` (smoke test) to confirm grammar loading + import extraction works for all four languages.
    11. Run `uv run mypy daydream/tree_sitter_index.py`.
    12. Run `uv run ruff check daydream/tree_sitter_index.py`.
  </action>
  <verify>
    <automated>uv run python -c "from daydream.tree_sitter_index import get_parser, LANGUAGES, extract_imports, PYTHON_IMPORT_QUERY; p1 = get_parser('python'); p2 = get_parser('python'); assert p1 is p2; assert LANGUAGES['.tsx'][0] == 'tsx'; assert LANGUAGES['.jsx'][0] == 'tsx'; assert LANGUAGES['.js'][0] == 'javascript'; imports = extract_imports(get_parser('python'), b'import os\nfrom collections import abc\n', PYTHON_IMPORT_QUERY); assert 'os' in imports, imports; assert any('collections' in i for i in imports), imports; print('OK')" &amp;&amp; uv run mypy daydream/tree_sitter_index.py &amp;&amp; uv run ruff check daydream/tree_sitter_index.py</automated>
  </verify>
  <done>
    - File `daydream/tree_sitter_index.py` exists.
    - LANGUAGES dict has all 7 extension entries.
    - Parser cache is keyed by language id and reuses instances.
    - Four query constants are defined.
    - Smoke verify command above prints OK.
    - mypy and ruff clean.
  </done>
  <acceptance_criteria>
    - `grep -c "^LANGUAGES" daydream/tree_sitter_index.py` >= 1.
    - `grep -c "PYTHON_IMPORT_QUERY\\|TYPESCRIPT_IMPORT_QUERY\\|GO_IMPORT_QUERY\\|RUST_IMPORT_QUERY" daydream/tree_sitter_index.py` >= 4.
    - `grep -c "_PARSER_CACHE" daydream/tree_sitter_index.py` >= 2.
    - `grep -c "def get_parser" daydream/tree_sitter_index.py` == 1.
    - `grep -c "def extract_imports" daydream/tree_sitter_index.py` == 1.
    - `grep -c "tree_sitter_python\\|tree_sitter_typescript\\|tree_sitter_go\\|tree_sitter_rust" daydream/tree_sitter_index.py` >= 4.
    - The verify command above exits 0 and prints `OK`.
    - `uv run mypy daydream/tree_sitter_index.py` exits 0.
    - `uv run ruff check daydream/tree_sitter_index.py` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement detect_affected_files() with diff parsing, 1-hop tracing, deleted-file safety, and unsupported-language fallback</name>
  <read_first>
    - daydream/tree_sitter_index.py (created in Task 1)
    - daydream/runner.py -- find `_git_diff()` to mirror the subprocess pattern; do NOT call `_git_diff()` from inside detect_affected_files (the diff is passed in as text)
    - .planning/phases/02-pre-scan-exploration/02-RESEARCH.md sections "Pattern 4: Pure / Sync Diff Parser", "Pitfall 4", "Pitfall 5", "git diff --name-status parsing"
    - tests/fixtures/diffs/*.diff (Wave 0 fixtures)
    - tests/test_tree_sitter_index.py (Wave 0 placeholders -- unmask them in this task)
  </read_first>
  <files>daydream/tree_sitter_index.py, tests/test_tree_sitter_index.py</files>
  <behavior>
    - `detect_affected_files(diff_text, repo_root, depth=1)` returns a list of FileInfo whose length is at least the number of changed files in the diff.
    - For `python_multifile.diff` (api.py imports models.py, both modified), the result includes both files with `role="modified"` and at least one `Dependency`-equivalent FileInfo entry showing the import edge (either via `role="imported_by"` on models.py or `role="imports"` on api.py -- pick one and document).
    - Same for typescript / go / rust fixtures.
    - For `trivial_single.diff` (one .md file), result has exactly 1 entry with `role="modified"`.
    - For a diff containing a `.rb` file, the .rb file appears with `role="modified"` and no dependency tracing happens for it (no exception).
    - For a diff with a `D` (deleted) status entry, the deleted file appears with `role="modified"` and `detect_affected_files` does NOT raise FileNotFoundError when the file is missing on disk.
    - Default `depth=1` means: traced imports of imports are NOT followed (only direct neighbors).
  </behavior>
  <action>
    1. In `daydream/tree_sitter_index.py`, add the public function:
       ```python
       def detect_affected_files(
           diff_text: str,
           repo_root: Path,
           depth: int = 1,
       ) -> list[FileInfo]:
       ```
       with a Google-style docstring documenting Args/Returns/Raises (raises nothing -- catches everything internally per D-06).
    2. Implement an internal helper `_parse_diff_name_status(diff_text: str) -> list[tuple[str, str]]` that extracts `(status_letter, path)` pairs from the diff text. Use a tolerant approach: scan lines starting with `diff --git ` to find file paths, and check for `deleted file mode` markers to flag status `"D"`. The tests use fixtures generated from real `git diff` so the format is stable. Status letters of interest: `A` (added), `M` (modified), `D` (deleted), `R` (renamed -- treat as modified for the new path).
    3. For each `(status, path)`:
       - Always create a `FileInfo(path=path, role="modified")` entry.
       - If `status == "D"`: skip the import-parsing branch entirely (Pitfall 4). Continue to next file.
       - Otherwise look up the language by extension via `LANGUAGES.get(Path(path).suffix)`. If None: continue (file recorded as modified, no tracing -- D-06).
       - Read the file from disk: `(repo_root / path).read_bytes()`. Wrap in try/except (FileNotFoundError, OSError) -- on failure, continue (file recorded as modified only).
       - Get the parser via `get_parser(language_id)`. Get the query via `_query_for_language(language_id)`. Run `extract_imports`. For each extracted import string, attempt to resolve it to a file path on disk:
         - **Python**: `from foo.bar import baz` -> try `repo_root/foo/bar.py`, `repo_root/foo/bar/__init__.py`, `repo_root/foo/bar/baz.py` (Pitfall 5). Add any that exist as `FileInfo(path=str(resolved.relative_to(repo_root)), role="imports")`.
         - **TypeScript/TSX/JS**: relative import string like `"./models"` -> resolve relative to the importer's directory, try suffixes `.ts`, `.tsx`, `.d.ts`, `index.ts`, `index.tsx`. Bare imports (no `./` or `../`) skip resolution.
         - **Go**: import path like `"example.com/m/models"` -> best-effort: search `repo_root` for a directory whose suffix matches the import path. If not found, skip. Document this as best-effort.
         - **Rust**: `use crate::models::User` -> resolve `crate::` to `src/` and try `models.rs` and `models/mod.rs`. Skip imports starting with `std::` or external crates.
       - For depth > 1: NOT supported in this plan. The `depth` parameter is reserved; the function asserts `depth == 1` and raises `NotImplementedError("depth > 1 reserved for future use")` if a caller passes anything else. Document this in the docstring.
    4. Also compute reverse edges (importers): for each modified file, grep the repo for files that import it. Use a simple `subprocess.run(["git", "-C", str(repo_root), "grep", "-l", "--", <module_name>], ...)` with `# noqa: S603` and a comment justifying the hardcoded args. For each match, add `FileInfo(path=match, role="imported_by")`. Wrap in try/except on subprocess errors -- empty result on failure.
    5. De-duplicate the resulting list by `(path, role)` tuple before returning.
    6. Edit `tests/test_tree_sitter_index.py`:
       - Remove the module-level `pytest.importorskip("daydream.tree_sitter_index")` from Wave 0.
       - Remove the `xfail` markers from all placeholder tests.
       - Flesh out each test body to load its fixture (`Path("tests/fixtures/diffs/<lang>_multifile.diff").read_text()`) and call `detect_affected_files(diff_text, repo_root=Path("."), depth=1)`. Use a `tmp_path` fixture or a small helper that materializes the fixture's source files into a tmp dir before calling -- otherwise the import resolution can't find them on disk. Concrete approach: write a `_materialize_fixture(tmp_path, fixture_name)` helper that creates the source files referenced in each diff fixture (api.py, models.py, etc.) with the post-diff content.
       - `test_default_depth_is_one` -- assert `inspect.signature(detect_affected_files).parameters["depth"].default == 1`.
       - `test_unsupported_language_gets_modified_role` -- write a small inline diff string adding `foo.rb`, materialize an empty foo.rb, call function, assert FileInfo for `foo.rb` exists with `role="modified"`.
       - `test_deleted_file_does_not_raise_filenotfound` -- diff with `D` status, do NOT materialize the file, call function, assert no exception and FileInfo for the deleted path is present with `role="modified"`.
    7. Run `uv run pytest tests/test_tree_sitter_index.py -v` -- all 7 tests must pass.
    8. Run `make check` to confirm lint+typecheck+full suite.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tree_sitter_index.py -v &amp;&amp; uv run mypy daydream &amp;&amp; uv run ruff check daydream</automated>
  </verify>
  <done>
    - `detect_affected_files` exists, returns `list[FileInfo]`, takes diff_text + repo_root + depth=1.
    - All 7 tests in tests/test_tree_sitter_index.py pass (no xfail, no skip).
    - Deleted files don't raise.
    - Unsupported languages return `role="modified"` only.
    - mypy clean, ruff clean.
    - Full suite still green.
  </done>
  <acceptance_criteria>
    - `grep -c "def detect_affected_files" daydream/tree_sitter_index.py` == 1.
    - `grep -c "depth: int = 1" daydream/tree_sitter_index.py` >= 1.
    - `grep -c "imported_by\\|imports" daydream/tree_sitter_index.py` >= 2.
    - `grep -c "FileNotFoundError\\|OSError" daydream/tree_sitter_index.py` >= 1.
    - `grep -c "noqa: S603" daydream/tree_sitter_index.py` >= 1 (for the git grep subprocess call).
    - `grep -c "importorskip" tests/test_tree_sitter_index.py` == 0.
    - `grep -c "xfail" tests/test_tree_sitter_index.py` == 0.
    - `uv run pytest tests/test_tree_sitter_index.py -v` reports 7 passed, 0 failed, 0 skipped, 0 xfailed.
    - `uv run mypy daydream` exits 0.
    - `uv run ruff check daydream` exits 0.
    - `uv run pytest -v` (full suite) exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All tree_sitter_index unit tests pass against the Wave 0 diff fixtures.
- Adding a future grammar requires only one LANGUAGES entry + one query constant + one factory function -- verify by reading the registry structure.
- Python, TS, Go, and Rust all parse imports correctly via tree-sitter Query (no regex anywhere).
</verification>

<success_criteria>
- EXPL-01 satisfied: `detect_affected_files()` returns the impact surface for multi-file diffs in all four supported languages.
- EXPL-02 satisfied: default depth = 1; direct imports and direct importers are returned, deeper hops are not followed.
- D-06 satisfied: unsupported languages and missing files degrade gracefully with `role="modified"` and no exception.
- `make check` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pre-scan-exploration/02-02-SUMMARY.md`.
</output>
