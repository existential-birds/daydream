---
phase: 01-vendor-atif-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-01-PLAN, 01-02-PLAN, 01-03-PLAN]
files_modified: []
autonomous: true
requirements: [VEND-04]
confidence: 0.95
tags: [phase-gate, verification, regression]

must_haves:
  goal: "Confirm the vendored Phase 1 deliverable lands without regressions: zero `from harbor` imports anywhere in the daydream tree, all 343 pre-existing tests still pass alongside the new smoke test, lint is clean, mypy is clean."
  truths:
    - "Across `daydream/` and `tests/`, zero Python source files contain `from harbor` or bare `import harbor` (NOTICE prose mentions of harbor are not code imports and don't count)"
    - "`make check` runs lint + typecheck + full pytest suite, all green"
    - "Pre-existing test count is preserved — 343 pre-existing tests pass + 5 new smoke test items = 348 passed total"
    - "Ruff per-file-ignores stanza for daydream/atif/** is actually being applied (verifiable via `uv run ruff check daydream/atif/models` exiting 0 even though vendored code is not lint-conformant)"
  verification:
    - "grep -rn 'from harbor\\|^import harbor' daydream/ tests/ --include='*.py' → no matches (exit 1)"
    - "uv run ruff check daydream → exits 0"
    - "uv run mypy daydream → exits 0"
    - "uv run pytest -v → reports '348 passed'"
    - "make check → exits 0"
---

<objective>
Phase-gate verification: prove the Phase 1 deliverable from Plans 01-03 is regression-free and ready to merge.

Purpose: VEND-04 requires "no remaining `from harbor import …` references anywhere in the daydream source tree." This plan also covers the implicit ROADMAP success criterion 5: "The existing 343-test suite still passes (vendoring is purely additive code; no production module imports from `daydream/atif/` yet)."

Output: A verification log (`.planning/phases/01-vendor-atif-foundation/01-04-SUMMARY.md`) recording the four gate checks: (1) zero Harbor imports, (2) ruff clean, (3) mypy clean, (4) full pytest suite green at the expected count. Touches NO files in the daydream tree — purely a series of read-only checks. If any check fails, the executor surfaces a checkpoint and does NOT silently fix; failures here likely mean Plans 01-03 missed something and need a fix-forward task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-vendor-atif-foundation/01-CONTEXT.md
@.planning/phases/01-vendor-atif-foundation/01-RESEARCH.md
@.planning/phases/01-vendor-atif-foundation/01-VALIDATION.md
@CLAUDE.md
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Run the four phase-gate checks and record outcomes</name>
  <files></files>
  <read_first>
    - Makefile (to confirm `make check` runs lint + typecheck + test as expected)
    - .planning/phases/01-vendor-atif-foundation/01-VALIDATION.md (lines 38-55 — per-task verification map; defines the gate commands)
    - .planning/phases/01-vendor-atif-foundation/01-RESEARCH.md (lines 750-775 — Phase Requirements -> Test Map; expected pytest counts)
  </read_first>
  <action>
    Run the four phase-gate checks IN ORDER. Record outcomes. If any check fails, STOP and surface a checkpoint — DO NOT modify code to make a check pass without explicit approval.

    **Check 1: Zero `from harbor` references in production daydream tree (VEND-04)**

    The daydream/atif/NOTICE file legitimately mentions `harbor` in prose (provenance line, attribution) — that's not a code import and should not count as a violation. Use the search below which scans only Python source and excludes documentation:

    ```bash
    # Source files only (Python). Catches all `from harbor.X` and bare `import harbor`.
    grep -rn 'from harbor\|^import harbor' daydream/ tests/ --include='*.py'
    # Expected: zero output, exit code 1
    ```

    Run it and capture the output. The acceptance criterion is **zero matches**. If matches are found:
    - If the match is inside `daydream/atif/models/` or `daydream/atif/validator.py`, Plan 01's sed substitution missed the file. Surface a checkpoint reading: "Plan 01 left a Harbor import at <file>:<line> — re-run the import-rename script and re-verify."
    - If the match is anywhere else (e.g., a test file accidentally imports from harbor), surface a checkpoint with the full grep output for human triage. Plan 1's scope did not touch production code, so any other location implies someone added a Harbor import outside this phase's scope.

    **Check 2: Ruff lint is clean across the entire daydream tree**

    ```bash
    uv run ruff check daydream
    # Expected: "All checks passed!" or no output, exit 0
    ```

    The `[tool.ruff.lint.per-file-ignores]` stanza from Plan 02 should silence rules for `daydream/atif/**` (vendored code). The daydream-authored shim (`daydream/atif/__init__.py`) IS covered by the per-file-ignores glob, but it should be lint-clean anyway because Plan 03 wrote it to daydream conventions. Plan 03's smoke test under `tests/` is NOT covered by the glob and IS subject to lint. Failure here means either:
    - The per-file-ignores stanza isn't being applied (Plan 02 issue — check the TOML syntax landed correctly)
    - Plan 03's daydream-authored code violates ruff rules (Plan 03 issue — fix in a follow-up task, do NOT silently edit here)

    Surface a checkpoint with the full ruff output if it exits non-zero.

    Also explicitly verify the per-file-ignores stanza is exempting vendored code (sanity check that the stanza glob hit) — `daydream/atif/models/` files are not formatted to daydream's style:

    ```bash
    uv run ruff check daydream/atif/models
    # Expected: "All checks passed!" exit 0 (because per-file-ignores hides any complaints)
    ```

    If this exits non-zero, the per-file-ignores stanza isn't matching — surface a checkpoint pointing at Plan 02.

    **Check 3: Mypy is clean across the entire daydream tree**

    ```bash
    uv run mypy daydream
    # Expected: "Success: no issues found in N source files", exit 0
    ```

    mypy uses `ignore_missing_imports = true` so any typo'd imports in vendored files would be silently ignored — but the smoke test from Plan 03 catches that at import time, so by Wave 3 we know imports resolve. Failure here likely means a type annotation issue in `daydream/atif/__init__.py` (the daydream-authored shim).

    Surface a checkpoint if it exits non-zero.

    **Check 4: Full pytest suite is green at expected count**

    ```bash
    uv run pytest -v 2>&1 | tee /tmp/phase01-pytest.log | tail -10
    # Expected last line: "==== 348 passed in NN.NNs ===="
    # (343 pre-existing + 5 new smoke items: 1 import + 2 parametrized goldens + 1 negative + 1 dict roundtrip)
    ```

    Validate the count:

    ```bash
    grep -E '^=+ [0-9]+ passed' /tmp/phase01-pytest.log
    # Expected: "==== 348 passed in NN.NNs ===="
    # The number 348 is non-negotiable: 343 (pre-existing baseline from STATE.md) + 5 (new smoke items)
    ```

    Failure modes:
    - **Count is 343 (no smoke tests collected)**: pytest didn't discover `tests/test_atif_vendor_smoke.py`. Check the file path is correct; check it's a `.py` file; check the file isn't being skipped. Surface a checkpoint.
    - **Count is 348 but with failures**: read /tmp/phase01-pytest.log, identify failing tests, surface a checkpoint with the full pytest failure output. DO NOT modify tests to make them pass.
    - **Count is < 343**: a pre-existing test broke as a side effect of vendoring. This shouldn't happen (vendoring is purely additive) but if it does, surface a checkpoint immediately — do NOT skip or modify the broken test.
    - **Count is > 348**: pytest discovered extra tests we didn't account for. Probably benign (e.g., conftest.py picked up a fixture that looks like a test) but record it and ask for human review.

    **Final aggregate check:**

    ```bash
    make check
    # Expected: exits 0
    ```

    This is the canonical CI gate. If individual checks 1-4 passed but `make check` failed, there's an inconsistency between targets — surface a checkpoint with the make output.

    **DO NOT** modify code to fix any failure. Each gate failure goes through a checkpoint. The Makefile does NOT need to change in this plan.
  </action>
  <verify>
    <automated>! grep -rn 'from harbor\|^import harbor' daydream/ tests/ --include='*.py' &amp;&amp; uv run ruff check daydream &amp;&amp; uv run mypy daydream &amp;&amp; uv run pytest -v 2&gt;&amp;1 | tee /tmp/phase01-pytest.log | grep -qE '^=+ 348 passed' &amp;&amp; make check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'from harbor\|^import harbor' daydream/ tests/ --include='*.py'` exits 1 (no matches)
    - `uv run ruff check daydream` exits 0
    - `uv run ruff check daydream/atif/models` exits 0 (proves per-file-ignores from Plan 02 is being applied)
    - `uv run mypy daydream` exits 0
    - `uv run pytest -v` outputs a final summary line matching exactly `^=+ 348 passed`
    - `/tmp/phase01-pytest.log` contains zero `failed` or `error` lines (`! grep -qE 'failed|error' /tmp/phase01-pytest.log` — modulo error-message-related test names which can be excluded by checking only the summary line)
    - `make check` exits 0
    - No source code under `daydream/` or `tests/` was modified by this plan (verifiable via `git diff --name-only daydream tests` showing zero changes since Plan 03 completed)
  </acceptance_criteria>
  <done>All four phase-gate checks pass; the Phase 1 deliverable is verified ready to merge. Zero regressions; zero Harbor imports; lint and typecheck clean; pytest at the expected 348-test count.</done>
</task>

<task type="auto">
  <name>Task 4.2: Write the phase summary</name>
  <files>.planning/phases/01-vendor-atif-foundation/01-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/01-vendor-atif-foundation/01-01-SUMMARY.md (if Plan 01 wrote one)
    - .planning/phases/01-vendor-atif-foundation/01-02-SUMMARY.md (if Plan 02 wrote one)
    - .planning/phases/01-vendor-atif-foundation/01-03-SUMMARY.md (if Plan 03 wrote one)
    - $HOME/.claude/get-shit-done/templates/summary.md (the SUMMARY template structure)
  </read_first>
  <action>
    Write `.planning/phases/01-vendor-atif-foundation/01-04-SUMMARY.md` capturing the phase-gate results from Task 4.1. The summary follows the template at `$HOME/.claude/get-shit-done/templates/summary.md`. Include:

    - **Status**: complete / blocked
    - **Verification log**: literal output (or trimmed tail) of each of the four checks from Task 4.1
    - **Pytest count**: the final summary line from `/tmp/phase01-pytest.log`
    - **Files added in Phase 1** (aggregated from Plans 01, 02, 03):
      - `daydream/atif/models/*.py` (11 files)
      - `daydream/atif/validator.py`
      - `daydream/atif/__init__.py`
      - `daydream/atif/LICENSE`
      - `daydream/atif/NOTICE`
      - `tests/fixtures/atif_golden/terminus2/hello-world-invalid-json.trajectory.json`
      - `tests/fixtures/atif_golden/openhands/hello-world.trajectory.json`
      - `tests/fixtures/atif_golden/_invalid/non-sequential-step-id.json`
      - `tests/test_atif_vendor_smoke.py`
    - **Files modified in Phase 1**: `pyproject.toml` (pydantic dep + ruff per-file-ignores)
    - **Requirement coverage**: VEND-01, VEND-02, VEND-03, VEND-04, VEND-05 (all 5 marked as completed)
    - **Provenance recorded**: Harbor v0.5.0, commit `5795e7638fbe0ee5d7923b6311df2c9f3747dcf0`, vendored on 2026-04-26
    - **Hand-off to Phase 2**: confirm `from daydream.atif import …` works; recorder authoring (CORE-01) is unblocked

    The summary is what STATE.md reads to update progress; keep it concise (~50 lines max). No need to repeat content from the per-plan summaries verbatim.

    Do NOT modify ROADMAP.md or REQUIREMENTS.md status checkboxes here — that's the orchestrator's job after the plan check.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-vendor-atif-foundation/01-04-SUMMARY.md &amp;&amp; grep -q '348 passed' .planning/phases/01-vendor-atif-foundation/01-04-SUMMARY.md &amp;&amp; grep -q 'VEND-01' .planning/phases/01-vendor-atif-foundation/01-04-SUMMARY.md &amp;&amp; grep -q 'VEND-05' .planning/phases/01-vendor-atif-foundation/01-04-SUMMARY.md &amp;&amp; grep -q '5795e7638fbe0ee5d7923b6311df2c9f3747dcf0' .planning/phases/01-vendor-atif-foundation/01-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-vendor-atif-foundation/01-04-SUMMARY.md` exists
    - Contains the literal substring `348 passed` (recording the gate-check 4 outcome)
    - Contains all 5 VEND-* requirement IDs (VEND-01, VEND-02, VEND-03, VEND-04, VEND-05)
    - Contains the Harbor commit SHA `5795e7638fbe0ee5d7923b6311df2c9f3747dcf0` (so the provenance is searchable)
    - Status block is clearly marked complete or blocked (one or the other; surfacing failures is OK)
  </acceptance_criteria>
  <done>SUMMARY.md is written, captures the phase-gate outcome, and provides the hand-off context for Phase 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 1 deliverable → Phase 2 consumers | The phase-gate verification is the contract handoff: if these checks pass, downstream phases can rely on the vendored surface |
| Test infrastructure → CI pipeline | `make check` mirrors the pre-push hook (`scripts/hooks/pre-push`) and CI; passing here means CI will pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Auto-fix temptation: executor silently modifies code to make a failing check pass | mitigate | Action explicitly forbids it ("DO NOT modify code to fix any failure"); each gate failure routes to a checkpoint with full diagnostic output, requiring human approval before remediation. |
| T-04-02 | Information Disclosure | Pytest output may contain sensitive test data in failure messages | accept | LOW — daydream's existing test suite is local-only; pytest output goes to /tmp/phase01-pytest.log on the dev box. Not exposed externally. |
| T-04-03 | Denial of Service | Slow `make check` (full 343-test suite + lint + mypy) blocks fast feedback during plan execution | accept | LOW — full suite runs ~30 seconds (per VALIDATION.md "Estimated runtime"). Sampling rate during sub-tasks is the smoke test only (<1s); full suite is intentionally a phase-gate-only check. |
| T-04-04 | Repudiation | Phase completion not recorded for STATE.md updates | mitigate | Task 4.2 writes a SUMMARY.md; the orchestrator reads SUMMARYs to update STATE.md/ROADMAP.md/REQUIREMENTS.md. |
| T-04-05 | Tampering | False-positive on harbor grep due to non-code mentions in NOTICE/docs | mitigate | grep is scoped with `--include='*.py'` so it only scans Python source. NOTICE prose mentions of "harbor" are not matched. |
</threat_model>

<verification>
After both tasks complete:

```bash
# Re-run the four phase-gate checks idempotently:
! grep -rn 'from harbor\|^import harbor' daydream/ tests/ --include='*.py'
uv run ruff check daydream
uv run mypy daydream
uv run pytest -v 2>&1 | grep -qE '^=+ 348 passed'
make check

# SUMMARY exists and is well-formed:
test -f .planning/phases/01-vendor-atif-foundation/01-04-SUMMARY.md
grep -q '348 passed' .planning/phases/01-vendor-atif-foundation/01-04-SUMMARY.md
```
</verification>

<success_criteria>
- All four phase-gate checks pass (no Harbor imports, ruff clean, mypy clean, 348 pytest passes)
- `make check` exits 0
- SUMMARY.md records the outcome and the Harbor provenance
- Phase 1 deliverable is ready to merge — Phase 2 (recorder authoring) is unblocked
- VEND-04 requirement satisfied
- Pre-existing 343-test baseline preserved (zero regressions from vendoring)
</success_criteria>

<output>
After completion, the orchestrator's `/gsd-verify-work` step will read this plan's SUMMARY plus the per-plan SUMMARYs from Plans 01-03 to update STATE.md, REQUIREMENTS.md, and ROADMAP.md. No further file authoring required.
</output>
